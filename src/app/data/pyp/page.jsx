'use client'

import { useState, useEffect, useMemo } from 'react'
import Link from 'next/link'
import { supabase } from '@/lib/supabase'
import { useTheme } from '@/lib/theme'
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import Modal from '@/components/ui/modal'
import NotificationModal from '@/components/ui/notification-modal'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import {
  faPlus,
  faBookOpen,
  faBrain,
  faGraduationCap,
  faFileAlt,
  faSpinner,
  faClock,
  faLightbulb,
  faCheck,
  faLayerGroup,
  faComments,
  faUserGraduate,
  faQuoteLeft,
  faPrint,
  faCopy,
  faEdit,
  faTrash,
  faDatabase,
  faListCheck,
  faInbox,
  faChalkboardUser,
  faChevronRight,
  faChevronLeft,
  faSearch,
  faInfoCircle,
  faKey,
  faTable,
  faAward,
  faSave,
  faCheckCircle,
  faExclamationTriangle
} from '@fortawesome/free-solid-svg-icons'
import { generatePypClassReportPDF } from './lib/pypPdfGenerator'

// PYP Transdisciplinary Themes (Standard IB PYP Framework)
const TRANSDISCIPLINARY_THEMES = [
  'Who We Are',
  'Where We Are in Place and Time',
  'How We Express Ourselves',
  'How The World Works',
  'How We Organize Ourselves',
  'Sharing the Planet'
]

// IB Learner Profile Attributes
const LEARNER_PROFILES = [
  'Inquirers', 'Knowledgeable', 'Thinkers', 'Communicators',
  'Principled', 'Open-Minded', 'Caring', 'Risk-Takers', 'Balanced', 'Reflective'
]

// PYP Subject Areas & Scope and Sequence Strands
const PYP_SUBJECT_AREAS = [
  {
    name: 'Language',
    code: 'LANG',
    strands: [
      { name: 'Oral Communication', desc: 'Listening and speaking' },
      { name: 'Written Communication — Reading', desc: 'Comprehension, interpretation, and literary appreciation' },
      { name: 'Written Communication — Writing', desc: 'Composition, mechanics, and creative expression' },
      { name: 'Visual Communication', desc: 'Viewing and presenting visual media' }
    ]
  },
  {
    name: 'Mathematics',
    code: 'MATH',
    strands: [
      { name: 'Data Handling', desc: 'Sorting, representing, and interpreting data' },
      { name: 'Measurement', desc: 'Estimating and measuring physical attributes' },
      { name: 'Shape and Space', desc: '2D/3D geometry and spatial awareness' },
      { name: 'Pattern and Function', desc: 'Algebraic thinking and number relationships' },
      { name: 'Number', desc: 'Counting, place value, operations, and mental math' }
    ]
  },
  {
    name: 'Science',
    code: 'SCI',
    strands: [
      { name: 'Living Things', desc: 'Biology, ecosystems, and human body' },
      { name: 'Earth and Space', desc: 'Geology, weather, astronomy, and environment' },
      { name: 'Materials and Matter', desc: 'Properties, states, and changes of matter' },
      { name: 'Forces and Energy', desc: 'Physics concepts, motion, light, sound, and electricity' }
    ]
  },
  {
    name: 'Social Studies',
    code: 'SOC-ST',
    strands: [
      { name: 'Human Systems & Economic Activities', desc: 'Governance, trade, and infrastructure' },
      { name: 'Social Organization & Culture', desc: 'Beliefs, traditions, and societal structures' },
      { name: 'Continuity & Change Through Time', desc: 'Historical perspectives and evolution' },
      { name: 'Human & Natural Environments', desc: 'Geography, resources, and sustainability' }
    ]
  },
  {
    name: 'Arts',
    code: 'ARTS',
    strands: [
      { name: 'Responding in Visual & Performing Arts', desc: 'Critique, reflection, and appreciation' },
      { name: 'Creating in Visual & Performing Arts', desc: 'Expression, technique, music, and drama' }
    ]
  },
  {
    name: 'PSPE (Personal, Social & Physical Ed.)',
    code: 'PSPE',
    strands: [
      { name: 'Identity', desc: 'Self-awareness, confidence, and personal growth' },
      { name: 'Active Living', desc: 'Physical health, fitness, and motor skill development' },
      { name: 'Interactions', desc: 'Teamwork, fair play, and community engagement' }
    ]
  }
]

// SQL Schema for PYP Strand Assessment Table
const SQL_ASSESSMENT_SCHEMA = `-- ==============================================================================
-- PYP STRAND ASSESSMENT / GRADING SCHEMA
-- Copy & Run in Supabase SQL Editor
-- ==============================================================================

-- 1. Ensure semester column exists in pyp_subject_strand (master strand applicability)
ALTER TABLE public.pyp_subject_strand 
ADD COLUMN IF NOT EXISTS semester VARCHAR(20) DEFAULT 'all';

-- 2. Create Strand Assessment Table with Semester support
CREATE TABLE IF NOT EXISTS public.pyp_strand_assessment (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    year_id BIGINT NOT NULL REFERENCES public.year(year_id) ON DELETE CASCADE,
    kelas_id BIGINT NOT NULL REFERENCES public.kelas(kelas_id) ON DELETE CASCADE,
    student_id BIGINT NOT NULL REFERENCES public.users(user_id) ON DELETE CASCADE,
    subject_id BIGINT NOT NULL REFERENCES public.pyp_subject(id) ON DELETE CASCADE,
    strand_id BIGINT NOT NULL REFERENCES public.pyp_subject_strand(id) ON DELETE CASCADE,
    semester INTEGER NOT NULL DEFAULT 1, -- 1 = Semester 1, 2 = Semester 2
    rating VARCHAR(50) NOT NULL DEFAULT 'N/A', -- 'N/A', 'Developing', 'Beginning', 'Achieving', 'Exceeding'
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    created_by BIGINT REFERENCES public.users(user_id) ON DELETE SET NULL,
    CONSTRAINT uq_pyp_strand_eval UNIQUE (year_id, kelas_id, student_id, strand_id, semester)
);

-- Migration for existing tables:
ALTER TABLE public.pyp_strand_assessment 
ADD COLUMN IF NOT EXISTS semester INTEGER NOT NULL DEFAULT 1;

ALTER TABLE public.pyp_strand_assessment 
DROP CONSTRAINT IF EXISTS uq_pyp_strand_eval;

ALTER TABLE public.pyp_strand_assessment 
ADD CONSTRAINT uq_pyp_strand_eval UNIQUE (year_id, kelas_id, student_id, strand_id, semester);

CREATE INDEX IF NOT EXISTS idx_pyp_strand_assess_lookup ON public.pyp_strand_assessment (year_id, kelas_id, subject_id, semester);
CREATE INDEX IF NOT EXISTS idx_pyp_strand_assess_student ON public.pyp_strand_assessment (student_id);

ALTER TABLE public.pyp_strand_assessment ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Allow read access to pyp_strand_assessment" ON public.pyp_strand_assessment;
CREATE POLICY "Allow read access to pyp_strand_assessment" ON public.pyp_strand_assessment FOR SELECT USING (true);

DROP POLICY IF EXISTS "Allow full access to pyp_strand_assessment" ON public.pyp_strand_assessment;
CREATE POLICY "Allow full access to pyp_strand_assessment" ON public.pyp_strand_assessment FOR ALL USING (true) WITH CHECK (true);
`

const PYP_COMMENT_TABLE_SQL = `
-- ==============================================================================
-- SQL SCHEMA MIGRATION: pyp_subject_comment
-- Teacher Comments per student per subject per semester for PYP
-- ==============================================================================

CREATE TABLE IF NOT EXISTS public.pyp_subject_comment (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    year_id BIGINT NOT NULL REFERENCES public.year(year_id) ON DELETE CASCADE,
    kelas_id BIGINT NOT NULL REFERENCES public.kelas(kelas_id) ON DELETE CASCADE,
    student_id BIGINT NOT NULL REFERENCES public.users(user_id) ON DELETE CASCADE,
    subject_id BIGINT NOT NULL REFERENCES public.pyp_subject(id) ON DELETE CASCADE,
    semester INTEGER NOT NULL CHECK (semester IN (1, 2)),
    comment_text TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    created_by BIGINT REFERENCES public.users(user_id) ON DELETE SET NULL,
    CONSTRAINT uq_pyp_subject_comment UNIQUE (year_id, kelas_id, student_id, subject_id, semester)
);

CREATE INDEX IF NOT EXISTS idx_pyp_subject_comment_lookup 
  ON public.pyp_subject_comment (year_id, kelas_id, subject_id, semester);

CREATE INDEX IF NOT EXISTS idx_pyp_subject_comment_student 
  ON public.pyp_subject_comment (student_id);

ALTER TABLE public.pyp_subject_comment ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Allow read access to pyp_subject_comment" ON public.pyp_subject_comment;
CREATE POLICY "Allow read access to pyp_subject_comment" ON public.pyp_subject_comment FOR SELECT USING (true);

DROP POLICY IF EXISTS "Allow full access to pyp_subject_comment" ON public.pyp_subject_comment;
CREATE POLICY "Allow full access to pyp_subject_comment" ON public.pyp_subject_comment FOR ALL USING (true) WITH CHECK (true);
`

const STRAND_RATING_OPTIONS = [
  { value: 'N/A', label: 'N/A' },
  { value: 'Beginning', label: 'Beginning' },
  { value: 'Developing', label: 'Developing' },
  { value: 'Achieving', label: 'Achieving' },
  { value: 'Exceeding', label: 'Exceeding' }
]

const getRatingBadgeStyle = (rating, isDark) => {
  switch (rating) {
    case 'Exceeding':
      return {
        bg: isDark ? 'rgba(124, 58, 237, 0.18)' : '#F5F3FF',
        text: isDark ? '#DDD6FE' : '#6D28D9',
        border: isDark ? '#8B5CF6' : '#DDD6FE'
      }
    case 'Achieving':
      return {
        bg: isDark ? 'rgba(16, 185, 129, 0.18)' : '#ECFDF5',
        text: isDark ? '#A7F3D0' : '#047857',
        border: isDark ? '#10B981' : '#A7F3D0'
      }
    case 'Developing':
      return {
        bg: isDark ? 'rgba(59, 130, 246, 0.18)' : '#EFF6FF',
        text: isDark ? '#BFDBFE' : '#1D4ED8',
        border: isDark ? '#3B82F6' : '#BFDBFE'
      }
    case 'Beginning':
      return {
        bg: isDark ? 'rgba(245, 158, 11, 0.18)' : '#FFFBEB',
        text: isDark ? '#FDE68A' : '#B45309',
        border: isDark ? '#F59E0B' : '#FDE68A'
      }
    default:
      return {
        bg: isDark ? '#27272A' : '#F4F4F5',
        text: isDark ? '#A1A1AA' : '#71717A',
        border: isDark ? '#3F3F46' : '#E4E4E7'
      }
  }
}

export default function PypPage() {
  const { theme, isDark } = useTheme()

  // Main 5 Tabs: 'templates' | 'poi' | 'atl' | 'subjects' | 'comment'
  const [activeTab, setActiveTab] = useState('poi')

  // Sub-tabs inside Tab 1 (Master Templates): 'ci' | 'loi' | 'atls' | 'kc'
  const [masterSubTab, setMasterSubTab] = useState('ci')

  // Filter State (Academic Year & Selected PYP Class)
  const [selectedYear, setSelectedYear] = useState('')
  const [selectedYearId, setSelectedYearId] = useState('')
  const [selectedClassId, setSelectedClassId] = useState('')
  
  // PYP Report Print Modal States
  const [showPrintModal, setShowPrintModal] = useState(false)
  const [printModalSemester, setPrintModalSemester] = useState('1')
  const [printScope, setPrintScope] = useState('class') // 'class' | 'single'
  const [printSingleStudentId, setPrintSingleStudentId] = useState('')
  const [printReportDate, setPrintReportDate] = useState('')
  const [printStudentsList, setPrintStudentsList] = useState([])
  const [loadingPrintStudents, setLoadingPrintStudents] = useState(false)
  const [isGeneratingPdf, setIsGeneratingPdf] = useState(false)

  // Handler to open print selection modal and fetch class students
  const handleOpenPrintModal = async (semester) => {
    if (!selectedClassId) {
      alert('Pilih kelas PYP terlebih dahulu.')
      return
    }
    setPrintModalSemester(semester)
    setPrintScope('class')
    setPrintReportDate('')
    setShowPrintModal(true)

    try {
      setLoadingPrintStudents(true)
      const { data: dsData, error: dsError } = await supabase
        .from('detail_siswa')
        .select('detail_siswa_id, detail_siswa_user_id')
        .eq('detail_siswa_kelas_id', Number(selectedClassId))

      if (!dsError && dsData && dsData.length > 0) {
        const uids = [...new Set(dsData.map(d => d.detail_siswa_user_id).filter(Boolean))]
        if (uids.length > 0) {
          const { data: usersData, error: uError } = await supabase
            .from('users')
            .select('user_id, user_nama_depan, user_nama_belakang, user_birth_date')
            .in('user_id', uids)

          if (!uError && usersData) {
            const userMap = new Map()
            usersData.forEach(u => userMap.set(u.user_id, u))

            const stds = dsData.map(ds => {
              const u = userMap.get(ds.detail_siswa_user_id)
              return {
                detail_siswa_id: ds.detail_siswa_id,
                user_id: ds.detail_siswa_user_id,
                user_nama_depan: u?.user_nama_depan || 'Unknown',
                user_nama_belakang: u?.user_nama_belakang || '',
                user_birth_date: u?.user_birth_date || null
              }
            }).sort((a, b) => (a.user_nama_depan || '').localeCompare(b.user_nama_depan || ''))

            setPrintStudentsList(stds)
            if (stds.length > 0) {
              setPrintSingleStudentId(stds[0].user_id.toString())
            }
          }
        } else {
          setPrintStudentsList([])
        }
      } else {
        setPrintStudentsList([])
      }
    } catch (err) {
      console.error('Error fetching students for print modal:', err)
    } finally {
      setLoadingPrintStudents(false)
    }
  }

  // Handler to execute PDF generation from modal settings
  const handleExecutePrintPdf = async () => {
    try {
      setIsGeneratingPdf(true)
      await generatePypClassReportPDF({
        classId: selectedClassId,
        className: currentSelectedClassObj?.kelas_nama,
        yearId: selectedYearId,
        yearName: selectedYear,
        semester: printModalSemester,
        targetStudentId: printScope === 'single' ? printSingleStudentId : 'all',
        customReportDate: printReportDate || null,
        onLoading: (isLoading) => setIsGeneratingPdf(isLoading),
        onError: (err) => alert('Gagal menghasilkan report PDF: ' + (err?.message || err))
      })
      setShowPrintModal(false)
    } catch (err) {
      console.error('Error generating PYP PDF:', err)
      alert('Gagal menghasilkan report PDF: ' + (err?.message || err))
    } finally {
      setIsGeneratingPdf(false)
    }
  }

  // Master Database Data States
  const [years, setYears] = useState([])
  const [units, setUnits] = useState([])
  const [pypClasses, setPypClasses] = useState([])
  const [subjects, setSubjects] = useState([])
  const [pypUnitsList, setPypUnitsList] = useState([]) // From dedicated `pyp_unit` table
  const [loading, setLoading] = useState(true)

  // Master Lists Data States (pyp_ci_list, pyp_loi_list, pyp_atls_list, pyp_kc_list)
  const [ciList, setCiList] = useState([])
  const [loiList, setLoiList] = useState([])
  const [atlsList, setAtlsList] = useState([])
  const [kcList, setKcList] = useState([])
  const [loadingMaster, setLoadingMaster] = useState(false)

  // Pivot Relationships States (pyploiunit, pypkcunit, pypatlsunit)
  const [loiPivots, setLoiPivots] = useState([])
  const [kcPivots, setKcPivots] = useState([])
  const [atlPivots, setAtlPivots] = useState([])

  // Master List Modals & Form
  const [showMasterModal, setShowMasterModal] = useState(false)
  const [editingItem, setEditingItem] = useState(null)
  const [masterFormData, setMasterFormData] = useState({ name: '', key: '', question: '', definition: '' })

  // Master Templates Search & Pagination States
  const [masterSearchQuery, setMasterSearchQuery] = useState('')
  const [masterPage, setMasterPage] = useState(1)
  const MASTER_ITEMS_PER_PAGE = 10

  const currentRawMasterList = useMemo(() => {
    if (masterSubTab === 'ci') return ciList
    if (masterSubTab === 'loi') return loiList
    if (masterSubTab === 'atls') return atlsList
    if (masterSubTab === 'kc') return kcList
    return []
  }, [masterSubTab, ciList, loiList, atlsList, kcList])

  const filteredMasterList = useMemo(() => {
    if (!masterSearchQuery.trim()) return currentRawMasterList
    const q = masterSearchQuery.toLowerCase().trim()
    return currentRawMasterList.filter(item => {
      const nameMatch = item.name && item.name.toLowerCase().includes(q)
      const keyMatch = item.key && item.key.toLowerCase().includes(q)
      const questionMatch = item.question && item.question.toLowerCase().includes(q)
      const defMatch = item.definition && item.definition.toLowerCase().includes(q)
      return nameMatch || keyMatch || questionMatch || defMatch
    })
  }, [currentRawMasterList, masterSearchQuery])

  const totalMasterPages = Math.max(Math.ceil(filteredMasterList.length / MASTER_ITEMS_PER_PAGE), 1)

  const paginatedMasterList = useMemo(() => {
    const start = (masterPage - 1) * MASTER_ITEMS_PER_PAGE
    return filteredMasterList.slice(start, start + MASTER_ITEMS_PER_PAGE)
  }, [filteredMasterList, masterPage])

  const handleMasterSubTabChange = (tabKey) => {
    setMasterSubTab(tabKey)
    setMasterSearchQuery('')
    setMasterPage(1)
  }

  // New PYP Class Unit Wizard Modal States
  const [showClassUnitModal, setShowClassUnitModal] = useState(false)
  const [wizardStep, setWizardStep] = useState(1) // Steps 1 to 4
  const [submittingUnit, setSubmittingUnit] = useState(false)
  const [notif, setNotif] = useState({ isOpen: false, title: '', message: '', type: 'success' })

  // Form State for New Unit of Inquiry in a Class
  const [unitFormData, setUnitFormData] = useState({
    title: '',
    centralIdea: '',
    theme: 'Who We Are',
    duration: '6'
  })
  const [selectedLoiIds, setSelectedLoiIds] = useState([])
  const [selectedKcIds, setSelectedKcIds] = useState([])
  const [selectedAtlIds, setSelectedAtlIds] = useState([])
  const [atlNotes, setAtlNotes] = useState({}) // { [atlId]: string }
  const [kcNotes, setKcNotes] = useState({}) // { [kcId]: string }
  const [showCiSuggestions, setShowCiSuggestions] = useState(false)
  const [selectedCiObj, setSelectedCiObj] = useState(null)
  const [searchCiQuery, setSearchCiQuery] = useState('')
  const [showLoiSuggestions, setShowLoiSuggestions] = useState(false)
  const [selectedLoiObjs, setSelectedLoiObjs] = useState([])
  const [searchLoiQuery, setSearchLoiQuery] = useState('')
  const [editingUnit, setEditingUnit] = useState(null)

  // Copy Unit Modal States
  const [showCopyModal, setShowCopyModal] = useState(false)
  const [copySourceYear, setCopySourceYear] = useState('')
  const [copySourceClassId, setCopySourceClassId] = useState('')
  const [copySourceUnitId, setCopySourceUnitId] = useState('')
  const [submittingCopy, setSubmittingCopy] = useState(false)

  // Comment Builder Assistant State
  const [commentData, setCommentData] = useState({
    studentName: '',
    strengths: 'Demonstrates outstanding inquiry skills during unit discussions and collaborates effectively with peers.',
    growth: 'Encouraged to improve daily reflection journal organization and time management.',
    actionPlan: 'Provide guided reflection templates and weekly homeroom teacher check-ins.',
    selectedProfile: 'Inquirers'
  })

  // -------------------------------------------------------------
  // SUBJECT & STRAND ASSESSMENT TAB STATES (Tab 3: Subject)
  // -------------------------------------------------------------
  const [masterPypSubjects, setMasterPypSubjects] = useState([])
  const [masterPypSubjectClasses, setMasterPypSubjectClasses] = useState([])
  const [masterPypStrands, setMasterPypStrands] = useState([])
  const [subjectTabSubjectId, setSubjectTabSubjectId] = useState('')
  const [classStudents, setClassStudents] = useState([])
  const [assessmentsMap, setAssessmentsMap] = useState({}) // { [`${studentId}_${strandId}_${semester}`]: 'Beginning' | ... }
  const [savingAssessments, setSavingAssessments] = useState(false)
  const [dbAssessSetupNeeded, setDbAssessSetupNeeded] = useState(false)
  const [copiedAssessSql, setCopiedAssessSql] = useState(false)
  const [studentSearchQuery, setStudentSearchQuery] = useState('')
  const [activeGradingSemester, setActiveGradingSemester] = useState('1') // '1' | '2'
  const [scopeConfirmModal, setScopeConfirmModal] = useState({
    isOpen: false,
    strandId: null,
    strandName: '',
    currentScope: '',
    targetScope: '',
    saving: false
  })

  // -------------------------------------------------------------
  // TEACHER COMMENT TAB STATES (Tab 4: Teacher Comment)
  // -------------------------------------------------------------
  const [studentCommentsMap, setStudentCommentsMap] = useState({}) // { [studentId]: commentText }
  const [originalCommentsMap, setOriginalCommentsMap] = useState({}) // track unsaved state
  const [savingComments, setSavingComments] = useState(false)
  const [dbCommentSetupNeeded, setDbCommentSetupNeeded] = useState(false)
  const [copiedCommentSql, setCopiedCommentSql] = useState(false)

  // Fetch Master PYP Subjects, Classes Junction, and Strands
  const fetchPypMasterCurriculum = async () => {
    try {
      const [resSub, resSC, resStrand] = await Promise.all([
        supabase.from('pyp_subject').select('*').eq('is_active', true).order('order_index', { ascending: true }).order('name', { ascending: true }),
        supabase.from('pyp_subject_class').select('*'),
        supabase.from('pyp_subject_strand').select('*').eq('is_active', true).order('order_index', { ascending: true }).order('name', { ascending: true })
      ])
      if (resSub.data) setMasterPypSubjects(resSub.data)
      if (resSC.data) setMasterPypSubjectClasses(resSC.data)
      if (resStrand.data) setMasterPypStrands(resStrand.data)
    } catch (err) {
      console.warn('PYP master subjects and strands fetch notice:', err)
    }
  }

  // Fetch Class Students from detail_siswa & users
  const fetchClassStudents = async (targetClassId) => {
    if (!targetClassId) {
      setClassStudents([])
      return
    }
    try {
      const { data: dsData, error: dsError } = await supabase
        .from('detail_siswa')
        .select('detail_siswa_id, detail_siswa_user_id')
        .eq('detail_siswa_kelas_id', Number(targetClassId))

      if (dsError) throw dsError

      if (dsData && dsData.length > 0) {
        const uids = [...new Set(dsData.map(d => d.detail_siswa_user_id).filter(Boolean))]
        if (uids.length > 0) {
          const { data: usersData, error: uError } = await supabase
            .from('users')
            .select('user_id, user_nama_depan, user_nama_belakang, user_birth_date')
            .in('user_id', uids)

          if (!uError && usersData) {
            const userMap = new Map()
            usersData.forEach(u => userMap.set(u.user_id, u))

            const students = dsData.map(ds => {
              const u = userMap.get(ds.detail_siswa_user_id)
              return {
                detail_siswa_id: ds.detail_siswa_id,
                user_id: ds.detail_siswa_user_id,
                user_nama_depan: u?.user_nama_depan || 'Unknown',
                user_nama_belakang: u?.user_nama_belakang || '',
                user_birth_date: u?.user_birth_date || null
              }
            }).sort((a, b) => (a.user_nama_depan || '').localeCompare(b.user_nama_depan || ''))

            setClassStudents(students)
          } else {
            setClassStudents([])
          }
        } else {
          setClassStudents([])
        }
      } else {
        setClassStudents([])
      }
    } catch (err) {
      console.error('Error fetching class students:', err)
      setClassStudents([])
    }
  }

  // Fetch Strand Assessments for current year, class, and subject
  const fetchStrandAssessments = async (yearId, classId, subjectId) => {
    if (!yearId || !classId || !subjectId) {
      setAssessmentsMap({})
      return
    }
    try {
      const { data, error } = await supabase
        .from('pyp_strand_assessment')
        .select('*')
        .eq('year_id', Number(yearId))
        .eq('kelas_id', Number(classId))
        .eq('subject_id', Number(subjectId))

      if (error) {
        if (error.code === '42P01' || error.message?.includes('does not exist') || error.message?.includes('relation "public.pyp_strand_assessment"')) {
          setDbAssessSetupNeeded(true)
          return
        }
        throw error
      }

      setDbAssessSetupNeeded(false)
      const map = {}
      for (const row of (data || [])) {
        const sem = row.semester || 1
        map[`${row.student_id}_${row.strand_id}_${sem}`] = row.rating || 'N/A'
        // Fallback for legacy lookups without semester suffix:
        map[`${row.student_id}_${row.strand_id}`] = row.rating || 'N/A'
      }
      setAssessmentsMap(map)
    } catch (err) {
      console.error('Error fetching strand assessments:', err)
    }
  }

  // Fetch PYP Subject Teacher Comments (Tab 4)
  const fetchPypSubjectComments = async (yearId, classId, subjectId, semester) => {
    if (!yearId || !classId || !subjectId) {
      setStudentCommentsMap({})
      setOriginalCommentsMap({})
      return
    }
    try {
      const { data, error } = await supabase
        .from('pyp_subject_comment')
        .select('*')
        .eq('year_id', Number(yearId))
        .eq('kelas_id', Number(classId))
        .eq('subject_id', Number(subjectId))
        .eq('semester', Number(semester || activeGradingSemester || 1))

      if (error) {
        if (error.code === '42P01' || error.message?.includes('does not exist') || error.message?.includes('relation "public.pyp_subject_comment"')) {
          setDbCommentSetupNeeded(true)
          return
        }
        throw error
      }

      setDbCommentSetupNeeded(false)
      const map = {}
      for (const row of (data || [])) {
        map[row.student_id] = row.comment_text || ''
      }
      setStudentCommentsMap(map)
      setOriginalCommentsMap({ ...map })
    } catch (err) {
      console.error('Error fetching pyp subject comments:', err)
    }
  }

  // Fetch Master Data & Auto-select active Academic Year and PYP Classes (is_pyp === true)
  useEffect(() => {
    async function loadPypData() {
      setLoading(true)
      try {
        const [resYears, resUnits, resClasses, resSubjects, resPypUnits, resTopics] = await Promise.all([
          supabase.from('year').select('*').order('year_name', { ascending: false }),
          supabase.from('unit').select('*').eq('is_pyp', true).order('unit_name', { ascending: true }),
          supabase.from('kelas').select('*').order('kelas_nama', { ascending: true }),
          supabase.from('subject').select('*').order('subject_name', { ascending: true }),
          supabase.from('pyp_unit').select('*').or('is_deleted.eq.0,is_deleted.is.null').order('id', { ascending: true }),
          supabase.from('topic').select('*').order('topic_urutan', { ascending: true })
        ])

        // 1. Academic Years & Active Date Match
        if (resYears.data && resYears.data.length > 0) {
          setYears(resYears.data)

          const todayStr = new Date().toISOString().split('T')[0]
          
          const currentActiveYear = resYears.data.find(y => {
            if (!y.start_date || !y.end_date) return false
            return todayStr >= y.start_date && todayStr <= y.end_date
          })

          const targetYear = currentActiveYear || resYears.data[0]
          setSelectedYear(targetYear.year_name)
          setSelectedYearId(targetYear.year_id.toString())
        }

        // 2. Filter Classes strictly belonging to PYP Units (is_pyp === true)
        const pypUnitsListRaw = resUnits.data || []
        setUnits(pypUnitsListRaw)

        const pypUnitIdSet = new Set(pypUnitsListRaw.map(u => u.unit_id))
        const pypClassesList = (resClasses.data || []).filter(k => k.kelas_unit_id && pypUnitIdSet.has(k.kelas_unit_id))
        
        setPypClasses(pypClassesList)
        if (pypClassesList.length > 0) {
          setSelectedClassId(pypClassesList[0].kelas_id.toString())
        }

        if (resSubjects.data) setSubjects(resSubjects.data)

        // Combine dedicated `pyp_unit` items with fallback `topic` items
        let combinedPypUnits = []
        if (resPypUnits.data && resPypUnits.data.length > 0) {
          combinedPypUnits = resPypUnits.data.map(u => ({
            id: u.id,
            uniqueKey: `pyp-${u.id}`,
            title: u.title,
            centralIdea: u.central_idea,
            theme: u.theme,
            durationWeeks: u.duration_weeks,
            kelasId: u.kelas_id,
            yearName: u.year_name,
            status: u.status || 'published',
            isDedicated: true
          }))
        }
        
        if (resTopics.data && resTopics.data.length > 0) {
          const topicPypUnits = resTopics.data.map(t => ({
            id: t.topic_id,
            uniqueKey: `topic-${t.topic_id}`,
            title: t.topic_nama,
            centralIdea: t.topic_statement,
            theme: t.topic_global_context,
            durationWeeks: t.topic_duration,
            kelasId: t.topic_kelas_id,
            yearName: t.topic_year,
            status: t.topic_status || 'published',
            isDedicated: false
          }))
          combinedPypUnits = [...combinedPypUnits, ...topicPypUnits]
        }

        setPypUnitsList(combinedPypUnits)
      } catch (err) {
        console.error('Error fetching PYP data:', err)
      } finally {
        setLoading(false)
      }
    }

    async function fetchMasterListsAndPivots() {
      setLoadingMaster(true)
      try {
        const [resCi, resLoi, resAtls, resKc, resLoiPivots, resKcPivots, resAtlPivots] = await Promise.all([
          supabase.from('pyp_ci_list').select('*').or('is_deleted.eq.0,is_deleted.is.null').order('name', { ascending: true }),
          supabase.from('pyp_loi_list').select('*').or('is_deleted.eq.0,is_deleted.is.null').order('id', { ascending: true }),
          supabase.from('pyp_atls_list').select('*').or('is_deleted.eq.0,is_deleted.is.null').order('id', { ascending: true }),
          supabase.from('pyp_kc_list').select('*').or('is_deleted.eq.0,is_deleted.is.null').order('id', { ascending: true }),
          supabase.from('pyploiunit').select('*').or('is_deleted.eq.0,is_deleted.is.null'),
          supabase.from('pypkcunit').select('*').or('is_deleted.eq.0,is_deleted.is.null'),
          supabase.from('pypatlsunit').select('*').or('is_deleted.eq.0,is_deleted.is.null')
        ])

        if (resCi.data) setCiList(resCi.data)
        if (resLoi.data) setLoiList(resLoi.data)
        if (resAtls.data) setAtlsList(resAtls.data)
        if (resKc.data) setKcList(resKc.data)

        if (resLoiPivots.data) setLoiPivots(resLoiPivots.data)
        if (resKcPivots.data) setKcPivots(resKcPivots.data)
        if (resAtlPivots.data) setAtlPivots(resAtlPivots.data)
      } catch (err) {
        console.warn('PYP master lists or pivot tables notice:', err)
      } finally {
        setLoadingMaster(false)
      }
    }

    loadPypData()
    fetchMasterListsAndPivots()
    fetchPypMasterCurriculum()
  }, [])

  // Handle Academic Year selection change
  const handleYearChange = (yearName) => {
    setSelectedYear(yearName)
    const foundYear = years.find(y => y.year_name === yearName)
    if (foundYear) {
      setSelectedYearId(foundYear.year_id.toString())
    } else {
      setSelectedYearId('')
    }
  }

  // Filter PYP Units of Inquiry by Academic Year & Selected PYP Class
  const classTopics = useMemo(() => {
    return pypUnitsList.filter(t => {
      if (selectedYear && t.yearName && t.yearName !== selectedYear) return false
      if (selectedClassId && t.kelasId && t.kelasId.toString() !== selectedClassId) return false
      return true
    })
  }, [pypUnitsList, selectedYear, selectedClassId])

  // Get current active Class object
  const currentSelectedClassObj = useMemo(() => {
    return pypClasses.find(c => c.kelas_id.toString() === selectedClassId)
  }, [pypClasses, selectedClassId])

  // -------------------------------------------------------------
  // SUBJECT TAB MEMOIZED SELECTORS & HANDLERS (Tab 3: Subject)
  // -------------------------------------------------------------
  
  // Available Subjects for selected Class in the current Academic Year
  const availableSubjectsForCurrentClass = useMemo(() => {
    if (!selectedClassId) {
      if (!selectedYearId) return masterPypSubjects
      return masterPypSubjects.filter(s => !s.year_id || String(s.year_id) === String(selectedYearId))
    }
    const subjectIdsForClass = new Set(
      masterPypSubjectClasses
        .filter(sc => String(sc.kelas_id) === String(selectedClassId))
        .map(sc => sc.subject_id)
    )
    const linked = masterPypSubjects.filter(s => subjectIdsForClass.has(s.id))
    if (linked.length > 0) {
      if (selectedYearId) {
        const yearMatched = linked.filter(s => String(s.year_id) === String(selectedYearId))
        if (yearMatched.length > 0) return yearMatched
      }
      return linked
    }
    // Fallback if no junction table entries
    if (selectedYearId) {
      return masterPypSubjects.filter(s => !s.year_id || String(s.year_id) === String(selectedYearId))
    }
    return masterPypSubjects
  }, [masterPypSubjects, masterPypSubjectClasses, selectedYearId, selectedClassId])

  // Strands for currently selected Subject in Tab 3 (automatically filtered by active grading semester)
  const currentSubjectStrands = useMemo(() => {
    if (!subjectTabSubjectId) return []
    const allForSub = masterPypStrands.filter(st => String(st.subject_id) === String(subjectTabSubjectId))
    return allForSub.filter(st => {
      const sem = String(st.semester || 'all')
      return sem === 'all' || sem === String(activeGradingSemester)
    })
  }, [masterPypStrands, subjectTabSubjectId, activeGradingSemester])

  // Total strand count for current subject without semester filter
  const totalStrandsForSubject = useMemo(() => {
    if (!subjectTabSubjectId) return 0
    return masterPypStrands.filter(st => String(st.subject_id) === String(subjectTabSubjectId)).length
  }, [masterPypStrands, subjectTabSubjectId])

  // Active Subject object
  const currentSelectedSubjectObj = useMemo(() => {
    return availableSubjectsForCurrentClass.find(s => String(s.id) === String(subjectTabSubjectId)) || null
  }, [availableSubjectsForCurrentClass, subjectTabSubjectId])

  // Filtered Students in class for search
  const filteredClassStudents = useMemo(() => {
    if (!studentSearchQuery.trim()) return classStudents
    const q = studentSearchQuery.toLowerCase()
    return classStudents.filter(s => 
      `${s.user_nama_depan || ''} ${s.user_nama_belakang || ''}`.toLowerCase().includes(q)
    )
  }, [classStudents, studentSearchQuery])

  // Auto-sync subjectTabSubjectId when available subjects list changes
  useEffect(() => {
    if (availableSubjectsForCurrentClass.length > 0) {
      const exists = availableSubjectsForCurrentClass.some(s => String(s.id) === String(subjectTabSubjectId))
      if (!exists) {
        setSubjectTabSubjectId(String(availableSubjectsForCurrentClass[0].id))
      }
    } else {
      setSubjectTabSubjectId('')
    }
  }, [availableSubjectsForCurrentClass, subjectTabSubjectId])

  // Auto-fetch class students when selectedClassId changes
  useEffect(() => {
    if (selectedClassId) {
      fetchClassStudents(selectedClassId)
    }
  }, [selectedClassId])

  // Auto-fetch assessments when year, class, or subject changes
  useEffect(() => {
    if (selectedYearId && selectedClassId && subjectTabSubjectId) {
      fetchStrandAssessments(selectedYearId, selectedClassId, subjectTabSubjectId)
    }
  }, [selectedYearId, selectedClassId, subjectTabSubjectId])

  // Auto-fetch teacher comments when year, class, subject, activeGradingSemester, or activeTab changes
  useEffect(() => {
    if (selectedYearId && selectedClassId && subjectTabSubjectId && activeTab === 'comment') {
      fetchPypSubjectComments(selectedYearId, selectedClassId, subjectTabSubjectId, activeGradingSemester)
    }
  }, [selectedYearId, selectedClassId, subjectTabSubjectId, activeGradingSemester, activeTab])

  // Rating Change Handler
  const handleRatingChange = async (studentId, strandId, newRating) => {
    const sem = Number(activeGradingSemester) || 1
    const key = `${studentId}_${strandId}_${sem}`
    setAssessmentsMap(old => ({ 
      ...old, 
      [key]: newRating,
      [`${studentId}_${strandId}`]: newRating 
    }))

    try {
      const payload = {
        year_id: Number(selectedYearId),
        kelas_id: Number(selectedClassId),
        student_id: Number(studentId),
        subject_id: Number(subjectTabSubjectId),
        strand_id: Number(strandId),
        semester: sem,
        rating: newRating,
        updated_at: new Date().toISOString()
      }

      const { error } = await supabase
        .from('pyp_strand_assessment')
        .upsert(payload, { onConflict: 'year_id,kelas_id,student_id,strand_id,semester' })

      if (error) {
        if (error.code === '42P01' || error.message?.includes('does not exist') || error.message?.includes('relation "public.pyp_strand_assessment"')) {
          setDbAssessSetupNeeded(true)
          throw new Error('Table pyp_strand_assessment does not exist in Supabase.')
        }
        if (error.code === '42703' || error.message?.includes('semester')) {
          setDbAssessSetupNeeded(true)
          throw new Error('Column "semester" does not exist in pyp_strand_assessment. Please run the SQL schema migration in Supabase.')
        }
        throw error
      }
    } catch (err) {
      console.error('Error saving strand assessment:', err)
      setNotif({
        isOpen: true,
        title: 'Save Assessment Notice',
        message: err.message || 'Could not save assessment rating.',
        type: 'error'
      })
    }
  }

  // Batch set all students for a specific strand column in the active semester
  const handleBatchSetStrandRatings = async (strandId, targetRating) => {
    if (!classStudents || classStudents.length === 0) return
    const sem = Number(activeGradingSemester) || 1

    const newMap = { ...assessmentsMap }
    const upsertRows = []

    for (const std of classStudents) {
      const key = `${std.user_id}_${strandId}_${sem}`
      newMap[key] = targetRating
      newMap[`${std.user_id}_${strandId}`] = targetRating

      upsertRows.push({
        year_id: Number(selectedYearId),
        kelas_id: Number(selectedClassId),
        student_id: Number(std.user_id),
        subject_id: Number(subjectTabSubjectId),
        strand_id: Number(strandId),
        semester: sem,
        rating: targetRating,
        updated_at: new Date().toISOString()
      })
    }

    setAssessmentsMap(newMap)

    try {
      setSavingAssessments(true)
      const { error } = await supabase
        .from('pyp_strand_assessment')
        .upsert(upsertRows, { onConflict: 'year_id,kelas_id,student_id,strand_id,semester' })

      if (error) {
        if (error.code === '42P01' || error.message?.includes('does not exist') || error.message?.includes('relation "public.pyp_strand_assessment"')) {
          setDbAssessSetupNeeded(true)
          throw new Error('Table pyp_strand_assessment does not exist in Supabase.')
        }
        if (error.code === '42703' || error.message?.includes('semester')) {
          setDbAssessSetupNeeded(true)
          throw new Error('Column "semester" does not exist in pyp_strand_assessment. Please run the SQL schema migration in Supabase.')
        }
        throw error
      }

      setNotif({
        isOpen: true,
        title: 'Batch Rating Applied',
        message: `All ${classStudents.length} students marked as "${targetRating}" for Semester ${sem}.`,
        type: 'success'
      })
    } catch (err) {
      console.error('Error applying batch strand ratings:', err)
    } finally {
      setSavingAssessments(false)
    }
  }

  // Scope label helper
  const getScopeLabel = (scope) => {
    if (scope === '1') return 'Semester 1'
    if (scope === '2') return 'Semester 2'
    return 'Both Semesters (Full Academic Year)'
  }

  // Request Strand Semester Scope Change (Triggers Confirmation Modal)
  const handleRequestToggleStrandSemester = (strand, targetSemester) => {
    const currentScope = String(strand.semester || 'all')
    if (currentScope === targetSemester) return // Already set

    setScopeConfirmModal({
      isOpen: true,
      strandId: strand.id,
      strandName: strand.name,
      currentScope: currentScope,
      targetScope: targetSemester,
      saving: false
    })
  }

  // Confirm and Execute Strand Semester Scope Change
  const handleConfirmStrandSemesterChange = async () => {
    const { strandId, targetScope } = scopeConfirmModal
    if (!strandId || !targetScope) return

    setScopeConfirmModal(prev => ({ ...prev, saving: true }))

    // Optimistic local update
    setMasterPypStrands(prev =>
      prev.map(st => st.id === strandId ? { ...st, semester: targetScope } : st)
    )

    try {
      const { error } = await supabase
        .from('pyp_subject_strand')
        .update({ semester: targetScope, updated_at: new Date().toISOString() })
        .eq('id', strandId)

      if (error) {
        if (error.code === '42703' || error.message?.includes('semester')) {
          setNotif({
            isOpen: true,
            title: 'Database Setup Required',
            message: 'Column "semester" does not exist in pyp_subject_strand. Please run the SQL schema migration in Supabase.',
            type: 'warning'
          })
          setScopeConfirmModal(prev => ({ ...prev, isOpen: false, saving: false }))
          return
        }
        throw error
      }

      setNotif({
        isOpen: true,
        title: 'Scope Updated',
        message: 'Strand semester scope updated successfully.',
        type: 'success'
      })
      setScopeConfirmModal({
        isOpen: false,
        strandId: null,
        strandName: '',
        currentScope: '',
        targetScope: '',
        saving: false
      })
    } catch (err) {
      console.error('Error updating strand semester scope:', err)
      setNotif({
        isOpen: true,
        title: 'Update Notice',
        message: err.message || 'Could not update strand semester scope.',
        type: 'error'
      })
      setScopeConfirmModal(prev => ({ ...prev, saving: false }))
    }
  }

  // Copy Schema helper
  const handleCopyAssessSql = () => {
    navigator.clipboard.writeText(SQL_ASSESSMENT_SCHEMA)
    setCopiedAssessSql(true)
    setTimeout(() => setCopiedAssessSql(false), 3000)
    setNotif({
      isOpen: true,
      title: 'SQL Copied',
      message: 'Database schema for pyp_strand_assessment copied to clipboard!',
      type: 'success'
    })
  }

  // Handle Save Teacher Comments for all students in active Subject & Semester
  const handleSaveStudentComments = async () => {
    if (!selectedYearId || !selectedClassId || !subjectTabSubjectId) {
      setNotif({
        isOpen: true,
        title: 'Missing Selection',
        message: 'Please select Academic Year, Class, and Subject first.',
        type: 'warning'
      })
      return
    }
    if (classStudents.length === 0) {
      setNotif({
        isOpen: true,
        title: 'No Students',
        message: 'No students found in this class to save comments for.',
        type: 'warning'
      })
      return
    }

    setSavingComments(true)
    try {
      const payload = classStudents.map(st => ({
        year_id: Number(selectedYearId),
        kelas_id: Number(selectedClassId),
        student_id: Number(st.user_id),
        subject_id: Number(subjectTabSubjectId),
        semester: Number(activeGradingSemester || 1),
        comment_text: (studentCommentsMap[st.user_id] || '').trim(),
        updated_at: new Date().toISOString()
      }))

      const { error } = await supabase
        .from('pyp_subject_comment')
        .upsert(payload, {
          onConflict: 'year_id,kelas_id,student_id,subject_id,semester'
        })

      if (error) {
        if (error.code === '42P01' || error.message?.includes('does not exist') || error.message?.includes('relation "public.pyp_subject_comment"')) {
          setDbCommentSetupNeeded(true)
          throw new Error('Table pyp_subject_comment does not exist in Supabase.')
        }
        throw error
      }

      setOriginalCommentsMap({ ...studentCommentsMap })
      setNotif({
        isOpen: true,
        title: 'Comments Saved',
        message: `Successfully saved Semester ${activeGradingSemester} comments for ${classStudents.length} students!`,
        type: 'success'
      })
    } catch (err) {
      console.error('Error saving teacher comments:', err)
      setNotif({
        isOpen: true,
        title: 'Save Failed',
        message: err.message || 'Failed to save comments to database.',
        type: 'error'
      })
    } finally {
      setSavingComments(false)
    }
  }

  // Handle Copy Comment SQL Schema
  const handleCopyCommentSql = () => {
    navigator.clipboard.writeText(PYP_COMMENT_TABLE_SQL)
    setCopiedCommentSql(true)
    setTimeout(() => setCopiedCommentSql(false), 3000)
    setNotif({
      isOpen: true,
      title: 'SQL Copied',
      message: 'Database schema for pyp_subject_comment copied to clipboard!',
      type: 'success'
    })
  }

  // Central Idea Search Autocomplete Suggestions (Sorted A-Z strictly from Master DB)
  const filteredCiSuggestions = useMemo(() => {
    const query = (searchCiQuery || '').trim().toLowerCase()
    const sorted = [...ciList].sort((a, b) => (a.name || '').localeCompare(b.name || ''))
    if (!query) return sorted
    return sorted.filter(ci => (ci.name || '').toLowerCase().includes(query))
  }, [ciList, searchCiQuery])

  // Lines of Inquiry Search Autocomplete Suggestions (Multi-Select, Sorted A-Z strictly from Master DB)
  const filteredLoiSuggestions = useMemo(() => {
    const query = (searchLoiQuery || '').trim().toLowerCase()
    const sorted = [...loiList].sort((a, b) => (a.name || '').localeCompare(b.name || ''))
    if (!query) return sorted
    return sorted.filter(loi => (loi.name || '').toLowerCase().includes(query))
  }, [loiList, searchLoiQuery])

  // Filter available units for Copy Source Selection
  const sourceAvailableUnits = useMemo(() => {
    return pypUnitsList.filter(u => {
      const classMatch = u.kelasId?.toString() === copySourceClassId
      const yearMatch = copySourceYear ? u.yearName === copySourceYear : true
      return classMatch && yearMatch
    })
  }, [pypUnitsList, copySourceClassId, copySourceYear])

  // Open Copy Unit Modal
  const handleOpenCopyModal = () => {
    setCopySourceYear(selectedYear || (years[0]?.year_name || '2025/2026'))
    setCopySourceClassId(selectedClassId || (pypClasses[0]?.kelas_id?.toString() || ''))
    setCopySourceUnitId('')
    setShowCopyModal(true)
  }

  // Execute Unit Copy with all Pivot Relationships (LOIs, KCs, ATLs)
  const handleExecuteCopyUnit = async () => {
    if (!copySourceUnitId) {
      setNotif({
        isOpen: true,
        title: 'Select Unit',
        message: 'Please select a source unit to copy.',
        type: 'error'
      })
      return
    }

    const sourceUnit = pypUnitsList.find(u => u.id.toString() === copySourceUnitId)
    if (!sourceUnit) return

    setSubmittingCopy(true)
    try {
      let createdUnitId = null
      let isDedicated = false

      // 1. Insert new Unit into pyp_unit (or topic fallback) for target class & target year
      const pypUnitPayload = {
        title: sourceUnit.title,
        central_idea: sourceUnit.centralIdea,
        theme: sourceUnit.theme,
        duration_weeks: sourceUnit.durationWeeks || 6,
        kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
        year_name: selectedYear || '2025/2026',
        status: 'published'
      }

      const { data: newPypUnit } = await supabase.from('pyp_unit').insert([pypUnitPayload]).select()

      if (newPypUnit && newPypUnit[0]) {
        createdUnitId = newPypUnit[0].id
        isDedicated = true
      } else {
        const topicPayload = {
          topic_nama: sourceUnit.title,
          topic_statement: sourceUnit.centralIdea,
          topic_global_context: sourceUnit.theme,
          topic_duration: sourceUnit.durationWeeks || 6,
          topic_kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
          topic_year: selectedYear || '2025/2026',
          topic_status: 'published'
        }
        const { data: newTopicArr } = await supabase.from('topic').insert([topicPayload]).select()
        if (newTopicArr && newTopicArr[0]) {
          createdUnitId = newTopicArr[0].topic_id
        }
      }

      if (createdUnitId) {
        // 2. Copy LOI pivot relationships (pyploiunit)
        const sourceLois = loiPivots.filter(p => p.unitId === sourceUnit.id && (p.is_deleted === 0 || p.is_deleted === null))
        if (sourceLois.length > 0) {
          const loiInserts = sourceLois.map(p => ({
            "unitId": createdUnitId,
            "loiId": p.loiId,
            is_deleted: 0
          }))
          const { data: insertedLois } = await supabase.from('pyploiunit').insert(loiInserts).select()
          if (insertedLois) setLoiPivots(prev => [...prev, ...insertedLois])
        }

        // 3. Copy Key Concept pivot relationships (pypkcunit)
        const sourceKcs = kcPivots.filter(p => p.unitId === sourceUnit.id && (p.is_deleted === 0 || p.is_deleted === null))
        if (sourceKcs.length > 0) {
          const kcInserts = sourceKcs.map(p => ({
            "unitId": createdUnitId,
            "kcId": p.kcId,
            is_deleted: 0
          }))
          const { data: insertedKcs } = await supabase.from('pypkcunit').insert(kcInserts).select()
          if (insertedKcs) setKcPivots(prev => [...prev, ...insertedKcs])
        }

        // 4. Copy ATL Skills pivot relationships (pypatlsunit)
        const sourceAtls = atlPivots.filter(p => p.unitId === sourceUnit.id && (p.is_deleted === 0 || p.is_deleted === null))
        if (sourceAtls.length > 0) {
          const atlInserts = sourceAtls.map(p => ({
            "unitId": createdUnitId,
            "atlId": p.atlId,
            keterangan: p.keterangan || '',
            is_deleted: 0
          }))
          const { data: insertedAtls } = await supabase.from('pypatlsunit').insert(atlInserts).select()
          if (insertedAtls) setAtlPivots(prev => [...prev, ...insertedAtls])
        }

        const newDisplayUnit = {
          id: createdUnitId,
          uniqueKey: isDedicated ? `pyp-${createdUnitId}` : `topic-${createdUnitId}`,
          title: sourceUnit.title,
          centralIdea: sourceUnit.centralIdea,
          theme: sourceUnit.theme,
          durationWeeks: sourceUnit.durationWeeks || 6,
          kelasId: selectedClassId ? parseInt(selectedClassId, 10) : null,
          yearName: selectedYear || '2025/2026',
          status: 'published',
          isDedicated: isDedicated
        }

        setPypUnitsList(prev => [...prev, newDisplayUnit])
      }

      setShowCopyModal(false)
      setNotif({
        isOpen: true,
        title: 'Unit Copied',
        message: `Successfully copied "${sourceUnit.title}" to ${currentSelectedClassObj?.kelas_nama || 'Class'} (${selectedYear || '2025/2026'}). All LOIs, Key Concepts, and ATL Skills have been replicated.`,
        type: 'success'
      })
    } catch (err) {
      console.error('Error copying unit:', err)
      setNotif({
        isOpen: true,
        title: 'Copy Failed',
        message: err.message || 'An error occurred while copying the unit.',
        type: 'error'
      })
    } finally {
      setSubmittingCopy(false)
    }
  }

  // Open New Class Unit Creation Wizard Modal
  const handleOpenClassUnitModal = () => {
    setEditingUnit(null)
    setWizardStep(1)
    setUnitFormData({
      title: '',
      centralIdea: '',
      theme: 'Who We Are',
      duration: '6'
    })
    setSelectedLoiIds([])
    setSelectedLoiObjs([])
    setSearchLoiQuery('')
    setShowLoiSuggestions(false)
    setSelectedKcIds([])
    setKcNotes({})
    setSelectedAtlIds([])
    setAtlNotes({})
    setSelectedCiObj(null)
    setSearchCiQuery('')
    setShowCiSuggestions(false)
    setShowClassUnitModal(true)
  }

  // Open Unit Editing Wizard Modal
  const handleEditClassUnit = (t) => {
    setEditingUnit(t)
    setWizardStep(1)
    setUnitFormData({
      title: t.title || '',
      centralIdea: t.centralIdea || '',
      theme: t.theme || 'Who We Are',
      duration: t.durationWeeks ? t.durationWeeks.toString() : '6'
    })

    // Match Central Idea object from ciList
    const foundCi = ciList.find(ci => ci.name === t.centralIdea)
    setSelectedCiObj(foundCi || { id: null, name: t.centralIdea })
    setSearchCiQuery(t.centralIdea || '')
    setShowCiSuggestions(false)

    // Match Lines of Inquiry pivots
    const linkedLoiRows = loiPivots.filter(p => p.unitId === t.id && (p.is_deleted === 0 || p.is_deleted === null))
    const loiIds = linkedLoiRows.map(p => Number(p.loiId))
    const linkedLoiObjs = loiList.filter(loi => loiIds.includes(loi.id))
    setSelectedLoiIds(loiIds)
    setSelectedLoiObjs(linkedLoiObjs)
    setSearchLoiQuery('')
    setShowLoiSuggestions(false)

    // Match Key Concepts pivots & notes
    const linkedKcRows = kcPivots.filter(p => p.unitId === t.id && (p.is_deleted === 0 || p.is_deleted === null))
    const kcIds = linkedKcRows.map(p => Number(p.kcId))
    const kcNotesMap = {}
    linkedKcRows.forEach(p => {
      kcNotesMap[Number(p.kcId)] = p.keterangan || ''
    })
    setSelectedKcIds(kcIds)
    setKcNotes(kcNotesMap)

    // Match ATL Skills pivots
    const linkedAtlRows = atlPivots.filter(p => p.unitId === t.id && (p.is_deleted === 0 || p.is_deleted === null))
    const atlIds = linkedAtlRows.map(p => Number(p.atlId))
    const notesMap = {}
    linkedAtlRows.forEach(p => {
      notesMap[Number(p.atlId)] = p.keterangan || ''
    })
    setSelectedAtlIds(atlIds)
    setAtlNotes(notesMap)

    setShowClassUnitModal(true)
  }

  // Toggle Checkbox Helpers
  const toggleLoiSelection = (id) => {
    setSelectedLoiIds(prev => prev.includes(id) ? prev.filter(item => item !== id) : [...prev, id])
  }

  const toggleKcSelection = (id) => {
    setSelectedKcIds(prev => prev.includes(id) ? prev.filter(item => item !== id) : [...prev, id])
  }

  const toggleAtlSelection = (id) => {
    setSelectedAtlIds(prev => prev.includes(id) ? prev.filter(item => item !== id) : [...prev, id])
  }

  // Save Complete Unit & Pivot Relationships to Database (Dedicated `pyp_unit` table first, with `topic` fallback)
  const handleSaveClassUnitWithPivots = async () => {
    if (!unitFormData.title.trim()) {
      setNotif({
        isOpen: true,
        title: 'Required Field Missing',
        message: 'Please provide a Unit Title.',
        type: 'error'
      })
      setWizardStep(1)
      return
    }

    if (!selectedCiObj) {
      setNotif({
        isOpen: true,
        title: 'Central Idea Required',
        message: 'Please select a Central Idea from the list.',
        type: 'error'
      })
      setWizardStep(1)
      return
    }

    if (selectedLoiIds.length === 0) {
      setNotif({
        isOpen: true,
        title: 'Lines of Inquiry Required',
        message: 'Please select at least 1 Line of Inquiry.',
        type: 'error'
      })
      setWizardStep(2)
      return
    }

    if (selectedKcIds.length === 0) {
      setNotif({
        isOpen: true,
        title: 'Key Concepts Required',
        message: 'Please select at least 1 Key Concept.',
        type: 'error'
      })
      setWizardStep(3)
      return
    }

    if (selectedAtlIds.length === 0) {
      setNotif({
        isOpen: true,
        title: 'ATL Skills Required',
        message: 'Please select at least 1 ATL Skill.',
        type: 'error'
      })
      setWizardStep(4)
      return
    }

    setSubmittingUnit(true)
    try {
      let unitIdToUse = null
      let isDedicated = false

      if (editingUnit) {
        unitIdToUse = editingUnit.id
        isDedicated = editingUnit.isDedicated

        if (isDedicated) {
          const pypUnitPayload = {
            title: unitFormData.title.trim(),
            central_idea: unitFormData.centralIdea.trim(),
            theme: unitFormData.theme,
            duration_weeks: parseInt(unitFormData.duration, 10) || 6,
            kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
            year_name: selectedYear || '2025/2026'
          }
          await supabase.from('pyp_unit').update(pypUnitPayload).eq('id', unitIdToUse)
        } else {
          const topicPayload = {
            topic_nama: unitFormData.title.trim(),
            topic_statement: unitFormData.centralIdea.trim(),
            topic_global_context: unitFormData.theme,
            topic_duration: parseInt(unitFormData.duration, 10) || 6,
            topic_kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
            topic_year: selectedYear || '2025/2026'
          }
          await supabase.from('topic').update(topicPayload).eq('topic_id', unitIdToUse)
        }

        // Clean old pivots for unitIdToUse before re-inserting updated ones
        await Promise.all([
          supabase.from('pyploiunit').delete().eq('unitId', unitIdToUse),
          supabase.from('pypkcunit').delete().eq('unitId', unitIdToUse),
          supabase.from('pypatlsunit').delete().eq('unitId', unitIdToUse)
        ])

        setLoiPivots(prev => prev.filter(p => p.unitId !== unitIdToUse))
        setKcPivots(prev => prev.filter(p => p.unitId !== unitIdToUse))
        setAtlPivots(prev => prev.filter(p => p.unitId !== unitIdToUse))
      } else {
        // Create new Unit
        const pypUnitPayload = {
          title: unitFormData.title.trim(),
          central_idea: unitFormData.centralIdea.trim(),
          theme: unitFormData.theme,
          duration_weeks: parseInt(unitFormData.duration, 10) || 6,
          kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
          year_name: selectedYear || '2025/2026',
          status: 'published'
        }

        const { data: newPypUnit } = await supabase.from('pyp_unit').insert([pypUnitPayload]).select()

        if (newPypUnit && newPypUnit[0]) {
          unitIdToUse = newPypUnit[0].id
          isDedicated = true
        } else {
          const topicPayload = {
            topic_nama: unitFormData.title.trim(),
            topic_statement: unitFormData.centralIdea.trim(),
            topic_global_context: unitFormData.theme,
            topic_duration: parseInt(unitFormData.duration, 10) || 6,
            topic_kelas_id: selectedClassId ? parseInt(selectedClassId, 10) : null,
            topic_year: selectedYear || '2025/2026',
            topic_status: 'published'
          }
          const { data: newTopicArr } = await supabase.from('topic').insert([topicPayload]).select()
          if (newTopicArr && newTopicArr[0]) {
            unitIdToUse = newTopicArr[0].topic_id
          }
        }
      }

      if (unitIdToUse) {
        // Save Selected Lines of Inquiry into pyploiunit
        if (selectedLoiIds.length > 0) {
          const loiInserts = selectedLoiIds.map(loiId => ({
            "unitId": Number(unitIdToUse),
            "loiId": Number(loiId),
            is_deleted: 0
          }))
          const { data: insertedLois, error: loiErr } = await supabase.from('pyploiunit').insert(loiInserts).select()
          if (loiErr) console.error('Error inserting pyploiunit:', loiErr)
          if (insertedLois) setLoiPivots(prev => [...prev, ...insertedLois])
        }

        // Save Selected Key Concepts into pypkcunit
        if (selectedKcIds.length > 0) {
          const kcInserts = selectedKcIds.map(kcId => ({
            "unitId": Number(unitIdToUse),
            "kcId": Number(kcId),
            is_deleted: 0
          }))
          const { data: insertedKcs, error: kcErr } = await supabase.from('pypkcunit').insert(kcInserts).select()
          if (kcErr) console.error('Error inserting pypkcunit:', kcErr)
          if (insertedKcs) setKcPivots(prev => [...prev, ...insertedKcs])
        }

        // Save Selected ATL Skills & Notes into pypatlsunit
        if (selectedAtlIds.length > 0) {
          const atlInserts = selectedAtlIds.map(atlId => ({
            "unitId": Number(unitIdToUse),
            "atlId": Number(atlId),
            keterangan: atlNotes[atlId] || '',
            is_deleted: 0
          }))
          const { data: insertedAtls, error: atlErr } = await supabase.from('pypatlsunit').insert(atlInserts).select()
          if (atlErr) console.error('Error inserting pypatlsunit:', atlErr)
          if (insertedAtls) setAtlPivots(prev => [...prev, ...insertedAtls])
        }

        const updatedDisplayUnit = {
          id: unitIdToUse,
          uniqueKey: isDedicated ? `pyp-${unitIdToUse}` : `topic-${unitIdToUse}`,
          title: unitFormData.title.trim(),
          centralIdea: unitFormData.centralIdea.trim(),
          theme: unitFormData.theme,
          durationWeeks: parseInt(unitFormData.duration, 10) || 6,
          kelasId: selectedClassId ? parseInt(selectedClassId, 10) : null,
          yearName: selectedYear || '2025/2026',
          status: 'published',
          isDedicated: isDedicated
        }

        if (editingUnit) {
          setPypUnitsList(prev => prev.map(u => u.id === unitIdToUse ? updatedDisplayUnit : u))
        } else {
          setPypUnitsList(prev => [...prev, updatedDisplayUnit])
        }
      }

      setShowClassUnitModal(false)
      setNotif({
        isOpen: true,
        title: editingUnit ? 'Unit Updated' : 'Unit Created',
        message: editingUnit ? `Unit "${unitFormData.title}" updated successfully.` : `Created Unit "${unitFormData.title}" for ${currentSelectedClassObj?.kelas_nama || 'Class'}.`,
        type: 'success'
      })
    } catch (err) {
      console.error('Error saving class unit:', err)
      setNotif({
        isOpen: true,
        title: 'Save Failed',
        message: err.message || 'An error occurred while saving the unit.',
        type: 'error'
      })
    } finally {
      setSubmittingUnit(false)
    }
  }

  // Open Master Item Modal (Create or Edit)
  const handleOpenMasterModal = (itemToEdit = null) => {
    setEditingItem(itemToEdit)
    if (itemToEdit) {
      setMasterFormData({
        name: itemToEdit.name || '',
        key: itemToEdit.key || '',
        question: itemToEdit.question || '',
        definition: itemToEdit.definition || ''
      })
    } else {
      setMasterFormData({ name: '', key: '', question: '', definition: '' })
    }
    setShowMasterModal(true)
  }

  // Save Master List Item directly to Supabase
  const handleSaveMasterItem = async (e) => {
    e.preventDefault()
    let tableName = 'pyp_ci_list'
    if (masterSubTab === 'loi') tableName = 'pyp_loi_list'
    if (masterSubTab === 'atls') tableName = 'pyp_atls_list'
    if (masterSubTab === 'kc') tableName = 'pyp_kc_list'

    try {
      if (masterSubTab === 'kc') {
        if (!masterFormData.key.trim()) return
        const payload = {
          key: masterFormData.key.trim(),
          question: masterFormData.question.trim(),
          definition: masterFormData.definition.trim()
        }

        if (editingItem) {
          const { error } = await supabase.from(tableName).update(payload).eq('id', editingItem.id)
          if (error) throw error
          
          setKcList(prev => prev.map(item => item.id === editingItem.id ? { ...item, ...payload } : item))
        } else {
          const { data, error } = await supabase.from(tableName).insert([payload]).select()
          if (error) throw error
          
          if (data && data[0]) {
            setKcList(prev => [...prev, data[0]])
          }
        }
      } else {
        if (!masterFormData.name.trim()) return
        const payload = { name: masterFormData.name.trim() }

        if (editingItem) {
          const { error } = await supabase.from(tableName).update(payload).eq('id', editingItem.id)
          if (error) throw error

          const updateState = (prev) => prev.map(item => item.id === editingItem.id ? { ...item, ...payload } : item)
          if (masterSubTab === 'ci') setCiList(updateState)
          if (masterSubTab === 'loi') setLoiList(updateState)
          if (masterSubTab === 'atls') setAtlsList(updateState)
        } else {
          const { data, error } = await supabase.from(tableName).insert([payload]).select()
          if (error) throw error

          if (data && data[0]) {
            const newObj = data[0]
            if (masterSubTab === 'ci') setCiList(prev => [...prev, newObj])
            if (masterSubTab === 'loi') setLoiList(prev => [...prev, newObj])
            if (masterSubTab === 'atls') setAtlsList(prev => [...prev, newObj])
          }
        }
      }

      setShowMasterModal(false)
      setNotif({
        isOpen: true,
        title: editingItem ? 'Item Updated' : 'Item Added',
        message: `Successfully saved item in database.`,
        type: 'success'
      })
    } catch (err) {
      console.error('Save master item error:', err)
      setNotif({
        isOpen: true,
        title: 'Save Failed',
        message: err.message || 'Could not save item to database.',
        type: 'error'
      })
    }
  }

  // Delete Master Item from Supabase
  const handleDeleteMasterItem = async (id) => {
    let tableName = 'pyp_ci_list'
    if (masterSubTab === 'loi') tableName = 'pyp_loi_list'
    if (masterSubTab === 'atls') tableName = 'pyp_atls_list'
    if (masterSubTab === 'kc') tableName = 'pyp_kc_list'

    try {
      const { error } = await supabase.from(tableName).update({ is_deleted: 1 }).eq('id', id)
      if (error) throw error
      
      const filterState = (prev) => prev.filter(item => item.id !== id)
      if (masterSubTab === 'ci') setCiList(filterState)
      if (masterSubTab === 'loi') setLoiList(filterState)
      if (masterSubTab === 'atls') setAtlsList(filterState)
      if (masterSubTab === 'kc') setKcList(filterState)

      setNotif({
        isOpen: true,
        title: 'Item Deleted',
        message: 'Master item removed successfully.',
        type: 'success'
      })
    } catch (err) {
      console.error('Delete error:', err)
      setNotif({
        isOpen: true,
        title: 'Delete Failed',
        message: err.message || 'Could not delete item.',
        type: 'error'
      })
    }
  }

  // Minimalist UI styling tokens
  const pageBg = isDark ? '#09090B' : '#FBFBFA'
  const cardBg = isDark ? '#18181B' : '#FFFFFF'
  const borderColor = isDark ? '#27272A' : '#EAEAEA'
  const textPrimary = isDark ? '#F4F4F5' : '#111111'
  const textSecondary = isDark ? '#A1A1AA' : '#787774'

  const inputStyle = {
    background: isDark ? '#27272A' : '#FFFFFF',
    border: `1px solid ${borderColor}`,
    color: textPrimary,
    borderRadius: '8px',
    fontSize: '13px'
  }

  const selectStyle = {
    background: isDark ? '#27272A' : '#FFFFFF',
    border: `1px solid ${borderColor}`,
    color: textPrimary,
    borderRadius: '8px',
    fontSize: '13px',
    padding: '8px 12px'
  }

  return (
    <div style={{ background: pageBg, minHeight: '100vh', padding: '32px 24px', color: textPrimary, fontFamily: "'Geist Sans', 'SF Pro Display', system-ui, -apple-system, sans-serif" }}>
      
      {/* ------------------------------------------------------------- */}
      {/* HEADER SECTION */}
      {/* ------------------------------------------------------------- */}
      <div style={{ maxWidth: '1200px', margin: '0 auto 32px auto' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', flexWrap: 'wrap', gap: '16px', marginBottom: '24px' }}>
          <div>
            <div style={{ display: 'inline-flex', alignItems: 'center', gap: '6px', padding: '4px 10px', borderRadius: '9999px', background: isDark ? 'rgba(59, 130, 246, 0.15)' : '#E1F3FE', color: isDark ? '#60A5FA' : '#1F6C9F', fontSize: '11px', fontWeight: 600, letterSpacing: '0.05em', textTransform: 'uppercase', marginBottom: '8px' }}>
              <FontAwesomeIcon icon={faGraduationCap} style={{ fontSize: '10px' }} />
              Primary Years Programme (PYP)
            </div>
            <h1 style={{ fontSize: '28px', fontWeight: 700, margin: 0, letterSpacing: '-0.02em', color: textPrimary }}>
              PYP Curriculum Framework
            </h1>
            <p style={{ margin: '6px 0 0 0', color: textSecondary, fontSize: '14px', lineHeight: 1.5 }}>
              Manage Master Templates, Class Units, Approaches to Learning, Subject Strands, and Teacher Comments.
            </p>
          </div>
        </div>

        {/* ------------------------------------------------------------- */}
        {/* ACADEMIC YEAR FILTER BAR ONLY */}
        {/* ------------------------------------------------------------- */}
        <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '10px', padding: '16px', marginBottom: '24px' }}>
          <div style={{ maxWidth: '300px' }}>
            <Label style={{ fontSize: '11px', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.05em', color: textSecondary, marginBottom: '6px', display: 'block' }}>
              Academic Year
            </Label>
            <select
              value={selectedYear}
              onChange={e => handleYearChange(e.target.value)}
              style={{ ...selectStyle, width: '100%' }}
            >
              <option value="">Select Academic Year</option>
              {years.map(y => (
                <option key={y.year_id} value={y.year_name}>{y.year_name}</option>
              ))}
            </select>
          </div>
        </div>

        {/* ------------------------------------------------------------- */}
        {/* TABS NAVIGATION (5 EXACT REQUESTED TABS) */}
        {/* ------------------------------------------------------------- */}
        <div style={{ display: 'flex', borderBottom: `1px solid ${borderColor}`, marginBottom: '24px', gap: '24px', flexWrap: 'wrap' }}>
          
          {/* TAB 1: Master Templates */}
          <button
            onClick={() => setActiveTab('templates')}
            style={{
              padding: '12px 0',
              fontSize: '14px',
              fontWeight: activeTab === 'templates' ? 600 : 400,
              border: 'none',
              background: 'none',
              cursor: 'pointer',
              color: activeTab === 'templates' ? textPrimary : textSecondary,
              borderBottom: activeTab === 'templates' ? `2px solid ${textPrimary}` : '2px solid transparent',
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              transition: 'all 0.15s ease'
            }}
          >
            <FontAwesomeIcon icon={faCopy} style={{ fontSize: '13px' }} />
            Master Templates
          </button>

          {/* TAB 2: Class */}
          <button
            onClick={() => setActiveTab('poi')}
            style={{
              padding: '12px 0',
              fontSize: '14px',
              fontWeight: activeTab === 'poi' ? 600 : 400,
              border: 'none',
              background: 'none',
              cursor: 'pointer',
              color: activeTab === 'poi' ? textPrimary : textSecondary,
              borderBottom: activeTab === 'poi' ? `2px solid ${textPrimary}` : '2px solid transparent',
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              transition: 'all 0.15s ease'
            }}
          >
            <FontAwesomeIcon icon={faChalkboardUser} style={{ fontSize: '13px' }} />
            Class ({pypClasses.length})
          </button>

          {/* TAB 3: Subject */}
          <button
            onClick={() => setActiveTab('subject')}
            style={{
              padding: '12px 0',
              fontSize: '14px',
              fontWeight: activeTab === 'subject' ? 600 : 400,
              border: 'none',
              background: 'none',
              cursor: 'pointer',
              color: activeTab === 'subject' ? textPrimary : textSecondary,
              borderBottom: activeTab === 'subject' ? `2px solid ${textPrimary}` : '2px solid transparent',
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              transition: 'all 0.15s ease'
            }}
          >
            <FontAwesomeIcon icon={faBookOpen} style={{ fontSize: '13px' }} />
            Subject
          </button>

          {/* TAB 5: Teacher's Comment */}
          <button
            onClick={() => setActiveTab('comment')}
            style={{
              padding: '12px 0',
              fontSize: '14px',
              fontWeight: activeTab === 'comment' ? 600 : 400,
              border: 'none',
              background: 'none',
              cursor: 'pointer',
              color: activeTab === 'comment' ? textPrimary : textSecondary,
              borderBottom: activeTab === 'comment' ? `2px solid ${textPrimary}` : '2px solid transparent',
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              transition: 'all 0.15s ease'
            }}
          >
            <FontAwesomeIcon icon={faComments} style={{ fontSize: '13px' }} />
            Teacher's Comment
          </button>

        </div>

        {/* ------------------------------------------------------------- */}
        {/* TAB 1: MASTER DATA LISTS (DB STRICT) */}
        {/* ------------------------------------------------------------- */}
        {activeTab === 'templates' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '24px' }}>
            
            <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '10px', padding: '20px' }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '16px', marginBottom: '20px' }}>
                <div>
                  <h3 style={{ fontSize: '16px', fontWeight: 600, margin: '0 0 4px 0', color: textPrimary }}>
                    PYP Master Curriculum Library
                  </h3>
                  <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                    Manage master lists directly from the database for Central Ideas, Lines of Inquiry, Approaches to Learning, and Key Concepts.
                  </p>
                </div>

                <Button
                  onClick={() => handleOpenMasterModal()}
                  style={{
                    background: textPrimary,
                    color: isDark ? '#09090B' : '#FFFFFF',
                    border: 'none',
                    borderRadius: '6px',
                    fontSize: '12px',
                    fontWeight: 600,
                    padding: '8px 14px'
                  }}
                >
                  <FontAwesomeIcon icon={faPlus} style={{ marginRight: '6px' }} />
                  Add {masterSubTab === 'ci' ? 'Central Idea' : masterSubTab === 'loi' ? 'Line of Inquiry' : masterSubTab === 'atls' ? 'ATL Skill' : 'Key Concept'}
                </Button>
              </div>

              {/* Master Data Sub-Nav Pills & Search Bar */}
              <div style={{ display: 'flex', flexDirection: 'column', gap: '12px', marginBottom: '20px' }}>
                <div style={{ display: 'flex', gap: '8px', background: isDark ? '#27272A' : '#F4F4F5', padding: '4px', borderRadius: '8px', overflowX: 'auto' }}>
                  <button
                    onClick={() => handleMasterSubTabChange('ci')}
                    style={{
                      flex: 1,
                      padding: '8px 12px',
                      fontSize: '13px',
                      fontWeight: masterSubTab === 'ci' ? 600 : 400,
                      border: 'none',
                      borderRadius: '6px',
                      background: masterSubTab === 'ci' ? cardBg : 'transparent',
                      color: masterSubTab === 'ci' ? textPrimary : textSecondary,
                      cursor: 'pointer',
                      whiteSpace: 'nowrap',
                      boxShadow: masterSubTab === 'ci' ? '0 1px 2px rgba(0,0,0,0.05)' : 'none',
                      transition: 'all 0.15s ease'
                    }}
                  >
                    1. Central Ideas ({ciList.length})
                  </button>

                  <button
                    onClick={() => handleMasterSubTabChange('loi')}
                    style={{
                      flex: 1,
                      padding: '8px 12px',
                      fontSize: '13px',
                      fontWeight: masterSubTab === 'loi' ? 600 : 400,
                      border: 'none',
                      borderRadius: '6px',
                      background: masterSubTab === 'loi' ? cardBg : 'transparent',
                      color: masterSubTab === 'loi' ? textPrimary : textSecondary,
                      cursor: 'pointer',
                      whiteSpace: 'nowrap',
                      boxShadow: masterSubTab === 'loi' ? '0 1px 2px rgba(0,0,0,0.05)' : 'none',
                      transition: 'all 0.15s ease'
                    }}
                  >
                    2. Lines of Inquiry ({loiList.length})
                  </button>

                  <button
                    onClick={() => handleMasterSubTabChange('atls')}
                    style={{
                      flex: 1,
                      padding: '8px 12px',
                      fontSize: '13px',
                      fontWeight: masterSubTab === 'atls' ? 600 : 400,
                      border: 'none',
                      borderRadius: '6px',
                      background: masterSubTab === 'atls' ? cardBg : 'transparent',
                      color: masterSubTab === 'atls' ? textPrimary : textSecondary,
                      cursor: 'pointer',
                      whiteSpace: 'nowrap',
                      boxShadow: masterSubTab === 'atls' ? '0 1px 2px rgba(0,0,0,0.05)' : 'none',
                      transition: 'all 0.15s ease'
                    }}
                  >
                    3. Approaches to Learning ({atlsList.length})
                  </button>

                  <button
                    onClick={() => handleMasterSubTabChange('kc')}
                    style={{
                      flex: 1,
                      padding: '8px 12px',
                      fontSize: '13px',
                      fontWeight: masterSubTab === 'kc' ? 600 : 400,
                      border: 'none',
                      borderRadius: '6px',
                      background: masterSubTab === 'kc' ? cardBg : 'transparent',
                      color: masterSubTab === 'kc' ? textPrimary : textSecondary,
                      cursor: 'pointer',
                      whiteSpace: 'nowrap',
                      boxShadow: masterSubTab === 'kc' ? '0 1px 2px rgba(0,0,0,0.05)' : 'none',
                      transition: 'all 0.15s ease'
                    }}
                  >
                    4. Key Concepts ({kcList.length})
                  </button>
                </div>

                {/* Search Bar Input */}
                <div style={{ position: 'relative', width: '100%' }}>
                  <FontAwesomeIcon icon={faSearch} style={{ position: 'absolute', left: '12px', top: '50%', transform: 'translateY(-50%)', color: textSecondary, fontSize: '13px' }} />
                  <Input
                    type="text"
                    placeholder={`Search ${masterSubTab === 'ci' ? 'Central Ideas' : masterSubTab === 'loi' ? 'Lines of Inquiry' : masterSubTab === 'atls' ? 'ATL Skills' : 'Key Concepts'}...`}
                    value={masterSearchQuery}
                    onChange={e => {
                      setMasterSearchQuery(e.target.value)
                      setMasterPage(1)
                    }}
                    style={{ ...inputStyle, paddingLeft: '34px', paddingRight: masterSearchQuery ? '32px' : '12px', fontSize: '13px' }}
                  />
                  {masterSearchQuery && (
                    <button
                      type="button"
                      onClick={() => {
                        setMasterSearchQuery('')
                        setMasterPage(1)
                      }}
                      style={{ position: 'absolute', right: '10px', top: '50%', transform: 'translateY(-50%)', background: 'none', border: 'none', color: textSecondary, cursor: 'pointer', fontSize: '12px' }}
                    >
                      ✕
                    </button>
                  )}
                </div>
              </div>

              {/* Master Data Items Display (STRICT DATABASE DATA) */}
              {loadingMaster ? (
                <div style={{ padding: '36px', textAlign: 'center', color: textSecondary }}>
                  <FontAwesomeIcon icon={faSpinner} spin style={{ fontSize: '18px', marginBottom: '8px' }} />
                  <p style={{ margin: 0, fontSize: '13px' }}>Loading master data from database...</p>
                </div>
              ) : filteredMasterList.length === 0 ? (
                <div style={{ border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '36px', textAlign: 'center' }}>
                  <FontAwesomeIcon icon={faInbox} style={{ fontSize: '24px', color: textSecondary, marginBottom: '8px' }} />
                  <p style={{ margin: '0 0 12px 0', fontSize: '13px', color: textSecondary }}>
                    {masterSearchQuery ? `No matching items found for "${masterSearchQuery}".` : 'No items found in database.'}
                  </p>
                  {!masterSearchQuery && (
                    <Button onClick={() => handleOpenMasterModal()} style={{ background: textPrimary, color: isDark ? '#09090B' : '#FFFFFF', fontSize: '12px', padding: '6px 14px' }}>
                      <FontAwesomeIcon icon={faPlus} style={{ marginRight: '6px' }} /> Add Item
                    </Button>
                  )}
                </div>
              ) : (
                <div>
                  {masterSubTab === 'kc' ? (
                    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(320px, 1fr))', gap: '14px' }}>
                      {paginatedMasterList.map((item, index) => {
                        const globalIdx = (masterPage - 1) * MASTER_ITEMS_PER_PAGE + index + 1
                        return (
                          <div key={item.id || index} style={{ background: isDark ? '#27272A' : '#FBFBFA', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '16px', display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
                            <div>
                              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '8px' }}>
                                <span style={{ fontSize: '12px', fontWeight: 700, padding: '3px 8px', borderRadius: '4px', background: isDark ? 'rgba(59, 130, 246, 0.15)' : '#E1F3FE', color: isDark ? '#60A5FA' : '#1F6C9F', fontFamily: 'monospace' }}>
                                  #{globalIdx} {item.key}
                                </span>
                                <div style={{ display: 'flex', gap: '6px' }}>
                                  <button onClick={() => handleOpenMasterModal(item)} style={{ background: 'none', border: 'none', color: textSecondary, cursor: 'pointer', padding: '2px' }}>
                                    <FontAwesomeIcon icon={faEdit} style={{ fontSize: '12px' }} />
                                  </button>
                                  <button onClick={() => handleDeleteMasterItem(item.id)} style={{ background: 'none', border: 'none', color: '#EF4444', cursor: 'pointer', padding: '2px' }}>
                                    <FontAwesomeIcon icon={faTrash} style={{ fontSize: '12px' }} />
                                  </button>
                                </div>
                              </div>

                              {item.question && (
                                <div style={{ fontSize: '12px', fontWeight: 600, color: textPrimary, marginBottom: '6px' }}>
                                  Question: &ldquo;{item.question}&rdquo;
                                </div>
                              )}

                              {item.definition && (
                                <p style={{ fontSize: '12px', color: textSecondary, margin: 0, lineHeight: 1.5 }}>
                                  {item.definition}
                                </p>
                              )}
                            </div>
                          </div>
                        )
                      })}
                    </div>
                  ) : (
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
                      {paginatedMasterList.map((item, index) => {
                        const globalIdx = (masterPage - 1) * MASTER_ITEMS_PER_PAGE + index + 1
                        return (
                          <div key={item.id || index} style={{ background: isDark ? '#27272A' : '#FBFBFA', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '14px 16px', display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: '12px' }}>
                            <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
                              <span style={{ width: '26px', height: '26px', borderRadius: '50%', background: isDark ? '#3F3F46' : '#E5E5E5', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: 600, color: textSecondary, flexShrink: 0 }}>
                                {globalIdx}
                              </span>
                              <span style={{ fontSize: '13px', color: textPrimary, lineHeight: 1.5 }}>
                                {item.name}
                              </span>
                            </div>

                            <div style={{ display: 'flex', gap: '6px', flexShrink: 0 }}>
                              <button onClick={() => handleOpenMasterModal(item)} style={{ background: 'none', border: 'none', color: textSecondary, cursor: 'pointer', padding: '4px' }}>
                                <FontAwesomeIcon icon={faEdit} style={{ fontSize: '12px' }} />
                              </button>
                              <button onClick={() => handleDeleteMasterItem(item.id)} style={{ background: 'none', border: 'none', color: '#EF4444', cursor: 'pointer', padding: '4px' }}>
                                <FontAwesomeIcon icon={faTrash} style={{ fontSize: '12px' }} />
                              </button>
                            </div>
                          </div>
                        )
                      })}
                    </div>
                  )}

                  {/* PAGINATION FOOTER BAR */}
                  {filteredMasterList.length > MASTER_ITEMS_PER_PAGE && (
                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: '20px', paddingTop: '16px', borderTop: `1px solid ${borderColor}`, flexWrap: 'wrap', gap: '12px' }}>
                      <div style={{ fontSize: '12px', color: textSecondary }}>
                        Showing <strong>{Math.min((masterPage - 1) * MASTER_ITEMS_PER_PAGE + 1, filteredMasterList.length)}–{Math.min(masterPage * MASTER_ITEMS_PER_PAGE, filteredMasterList.length)}</strong> of <strong>{filteredMasterList.length}</strong> items
                      </div>

                      <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
                        <Button
                          type="button"
                          disabled={masterPage === 1}
                          onClick={() => setMasterPage(prev => Math.max(prev - 1, 1))}
                          style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '12px', padding: '6px 12px', height: '32px' }}
                        >
                          <FontAwesomeIcon icon={faChevronLeft} style={{ marginRight: '4px' }} /> Prev
                        </Button>

                        {Array.from({ length: totalMasterPages }, (_, i) => i + 1).map(pNum => (
                          <button
                            key={pNum}
                            onClick={() => setMasterPage(pNum)}
                            style={{
                              width: '32px',
                              height: '32px',
                              borderRadius: '6px',
                              border: `1px solid ${pNum === masterPage ? textPrimary : borderColor}`,
                              background: pNum === masterPage ? textPrimary : 'transparent',
                              color: pNum === masterPage ? (isDark ? '#09090B' : '#FFFFFF') : textPrimary,
                              fontWeight: pNum === masterPage ? 700 : 400,
                              fontSize: '12px',
                              cursor: 'pointer',
                              transition: 'all 0.15s ease'
                            }}
                          >
                            {pNum}
                          </button>
                        ))}

                        <Button
                          type="button"
                          disabled={masterPage >= totalMasterPages}
                          onClick={() => setMasterPage(prev => Math.min(prev + 1, totalMasterPages))}
                          style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '12px', padding: '6px 12px', height: '32px' }}
                        >
                          Next <FontAwesomeIcon icon={faChevronRight} style={{ marginLeft: '4px' }} />
                        </Button>
                      </div>
                    </div>
                  )}
                </div>
              )}

            </div>
          </div>
        )}

        {/* ------------------------------------------------------------- */}
        {/* TAB 2: CLASS (FILTERED STRICTLY BY is_pyp === true) */}
        {/* ------------------------------------------------------------- */}
        {activeTab === 'poi' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '24px' }}>
            
            {/* PYP Class Selector Bar */}
            <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '10px', padding: '20px' }}>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '16px' }}>
                <div>
                  <h3 style={{ fontSize: '16px', fontWeight: 600, margin: '0 0 4px 0', color: textPrimary }}>
                    PYP School Classes
                  </h3>
                  <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                    Select a PYP Class to manage its Units of Inquiry.
                  </p>
                </div>

                <div style={{ minWidth: '240px', maxWidth: '360px', width: '100%' }}>
                  <select
                    value={selectedClassId || ''}
                    onChange={(e) => setSelectedClassId(e.target.value)}
                    style={{
                      ...selectStyle,
                      width: '100%',
                      padding: '10px 14px',
                      fontWeight: 600,
                      cursor: 'pointer'
                    }}
                  >
                    {pypClasses.length === 0 ? (
                      <option value="">No PYP classes found</option>
                    ) : (
                      pypClasses.map(cls => (
                        <option key={cls.kelas_id} value={cls.kelas_id.toString()}>
                          {cls.kelas_nama}
                        </option>
                      ))
                    )}
                  </select>
                </div>
              </div>
            </div>

            {/* Units Display for Selected PYP Class */}
            {selectedClassId && (
              <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '10px', padding: '24px' }}>
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '20px', flexWrap: 'wrap', gap: '12px' }}>
                  <div>
                    <h3 style={{ fontSize: '16px', fontWeight: 600, margin: '0 0 4px 0', color: textPrimary }}>
                      Units of Inquiry for {currentSelectedClassObj?.kelas_nama} ({classTopics.length})
                    </h3>
                    <span style={{ fontSize: '12px', color: textSecondary }}>
                      Academic Year: {selectedYear || 'All'}
                    </span>
                  </div>
                  
                  {/* Header Quick Print Actions */}
                  <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                    <button
                      type="button"
                      onClick={() => handleOpenPrintModal('1')}
                      style={{
                        background: isDark ? 'rgba(59, 130, 246, 0.12)' : '#EFF6FF',
                        border: `1px solid ${isDark ? 'rgba(59, 130, 246, 0.3)' : '#BFDBFE'}`,
                        color: isDark ? '#93C5FD' : '#1D4ED8',
                        borderRadius: '6px',
                        fontWeight: 600,
                        fontSize: '12px',
                        padding: '6px 12px',
                        display: 'inline-flex',
                        alignItems: 'center',
                        gap: '6px',
                        cursor: 'pointer',
                        transition: 'all 0.15s ease'
                      }}
                    >
                      <FontAwesomeIcon icon={faPrint} style={{ fontSize: '11px' }} />
                      <span>Print Report Sem 1</span>
                    </button>

                    <button
                      type="button"
                      onClick={() => handleOpenPrintModal('2')}
                      style={{
                        background: isDark ? 'rgba(168, 85, 247, 0.12)' : '#FAF5FF',
                        border: `1px solid ${isDark ? 'rgba(168, 85, 247, 0.3)' : '#E9D5FF'}`,
                        color: isDark ? '#C084FC' : '#7E22CE',
                        borderRadius: '6px',
                        fontWeight: 600,
                        fontSize: '12px',
                        padding: '6px 12px',
                        display: 'inline-flex',
                        alignItems: 'center',
                        gap: '6px',
                        cursor: 'pointer',
                        transition: 'all 0.15s ease'
                      }}
                    >
                      <FontAwesomeIcon icon={faPrint} style={{ fontSize: '11px' }} />
                      <span>Print Report Sem 2</span>
                    </button>
                  </div>
                </div>

                {loading ? (
                  <div style={{ padding: '48px', textAlign: 'center', color: textSecondary }}>
                    <FontAwesomeIcon icon={faSpinner} spin style={{ fontSize: '20px', marginBottom: '12px' }} />
                    <p style={{ margin: 0, fontSize: '13px' }}>Loading class units...</p>
                  </div>
                ) : (
                  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(320px, 1fr))', gap: '16px' }}>
                    
                    {/* SINGLE COMBINED ACTION CARD: CREATE, COPY UNIT & PRINT REPORTS */}
                    <div
                      style={{
                        background: isDark ? 'rgba(39, 39, 42, 0.4)' : '#FAF9F5',
                        border: `2px dashed ${borderColor}`,
                        borderRadius: '10px',
                        padding: '24px 20px',
                        display: 'flex',
                        flexDirection: 'column',
                        alignItems: 'center',
                        justifyContent: 'center',
                        textAlign: 'center',
                        minHeight: '220px',
                        gap: '14px'
                      }}
                    >
                      <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px' }}>
                        <h4 style={{ fontSize: '15px', fontWeight: 600, margin: 0, color: textPrimary }}>
                          Manage Class Units
                        </h4>
                        <p style={{ fontSize: '12px', color: textSecondary, margin: 0, lineHeight: 1.3, maxWidth: '220px' }}>
                          Create a new Unit of Inquiry, duplicate an existing one, or print report cards.
                        </p>
                      </div>

                      <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', width: '100%', maxWidth: '240px' }}>
                        <Button
                          onClick={handleOpenClassUnitModal}
                          style={{
                            width: '100%',
                            background: textPrimary,
                            color: isDark ? '#09090B' : '#FFFFFF',
                            border: 'none',
                            borderRadius: '6px',
                            fontWeight: 600,
                            fontSize: '13px',
                            padding: '9px 14px',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            gap: '8px'
                          }}
                        >
                          <FontAwesomeIcon icon={faPlus} />
                          Create New Unit
                        </Button>

                        <Button
                          onClick={handleOpenCopyModal}
                          style={{
                            width: '100%',
                            background: cardBg,
                            border: `1px solid ${borderColor}`,
                            color: textPrimary,
                            borderRadius: '6px',
                            fontWeight: 600,
                            fontSize: '12px',
                            padding: '8px 14px',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            gap: '8px'
                          }}
                        >
                          <FontAwesomeIcon icon={faCopy} />
                          Copy Unit
                        </Button>

                        {/* Divider */}
                        <div style={{ width: '100%', height: '1px', background: borderColor, margin: '2px 0' }} />

                        {/* Print Report Semester 1 */}
                        <Button
                          onClick={() => handleOpenPrintModal('1')}
                          style={{
                            width: '100%',
                            background: isDark ? 'rgba(59, 130, 246, 0.12)' : '#EFF6FF',
                            border: `1px solid ${isDark ? 'rgba(59, 130, 246, 0.3)' : '#BFDBFE'}`,
                            color: isDark ? '#93C5FD' : '#1D4ED8',
                            borderRadius: '6px',
                            fontWeight: 600,
                            fontSize: '12px',
                            padding: '8px 14px',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            gap: '8px',
                            cursor: 'pointer'
                          }}
                        >
                          <FontAwesomeIcon icon={faPrint} />
                          Print Report Semester 1
                        </Button>

                        {/* Print Report Semester 2 */}
                        <Button
                          onClick={() => handleOpenPrintModal('2')}
                          style={{
                            width: '100%',
                            background: isDark ? 'rgba(168, 85, 247, 0.12)' : '#FAF5FF',
                            border: `1px solid ${isDark ? 'rgba(168, 85, 247, 0.3)' : '#E9D5FF'}`,
                            color: isDark ? '#C084FC' : '#7E22CE',
                            borderRadius: '6px',
                            fontWeight: 600,
                            fontSize: '12px',
                            padding: '8px 14px',
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            gap: '8px',
                            cursor: 'pointer'
                          }}
                        >
                          <FontAwesomeIcon icon={faPrint} />
                          Print Report Semester 2
                        </Button>
                      </div>
                    </div>

                    {/* EXISTING CREATED UNIT CARDS */}
                    {classTopics.map(t => {
                      // Find linked pivot LOIs, Key Concepts, and ATLs for this topic
                      const linkedLois = loiPivots.filter(p => p.unitId === t.id)
                      const linkedKcs = kcPivots.filter(p => p.unitId === t.id)
                      const linkedAtls = atlPivots.filter(p => p.unitId === t.id)

                      return (
                        <div
                          key={t.uniqueKey || `${t.isDedicated ? 'pyp' : 'topic'}-${t.id}`}
                          onClick={() => handleEditClassUnit(t)}
                          style={{
                            background: isDark ? '#27272A' : '#FBFBFA',
                            border: `1px solid ${borderColor}`,
                            borderRadius: '10px',
                            padding: '20px',
                            display: 'flex',
                            flexDirection: 'column',
                            justify: 'space-between',
                            cursor: 'pointer',
                            transition: 'all 0.15s ease',
                            position: 'relative'
                          }}
                          onMouseEnter={e => {
                            e.currentTarget.style.borderColor = textPrimary
                            e.currentTarget.style.boxShadow = '0 4px 12px rgba(0,0,0,0.06)'
                          }}
                          onMouseLeave={e => {
                            e.currentTarget.style.borderColor = borderColor
                            e.currentTarget.style.boxShadow = 'none'
                          }}
                        >
                          <div>
                            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px' }}>
                              <span style={{ fontSize: '11px', fontWeight: 600, padding: '3px 8px', borderRadius: '9999px', background: isDark ? 'rgba(34, 197, 94, 0.15)' : '#EDF3EC', color: isDark ? '#4ADE80' : '#346538', textTransform: 'uppercase', letterSpacing: '0.05em' }}>
                                {t.theme || 'Who We Are'}
                              </span>
                              <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
                                {t.durationWeeks && (
                                  <span style={{ fontSize: '12px', color: textSecondary, display: 'flex', alignItems: 'center', gap: '4px' }}>
                                    <FontAwesomeIcon icon={faClock} style={{ fontSize: '10px' }} />
                                    {t.durationWeeks} Weeks
                                  </span>
                                )}
                                <span style={{ fontSize: '11px', fontWeight: 600, color: isDark ? '#60A5FA' : '#1F6C9F', display: 'flex', alignItems: 'center', gap: '4px' }}>
                                  <FontAwesomeIcon icon={faEdit} style={{ fontSize: '10px' }} /> Edit
                                </span>
                              </div>
                            </div>

                            <h4 style={{ fontSize: '16px', fontWeight: 600, margin: '0 0 8px 0', color: textPrimary, lineHeight: 1.4 }}>
                              {t.title}
                            </h4>

                            {t.centralIdea && (
                              <p style={{ fontSize: '13px', color: textSecondary, margin: '0 0 14px 0', lineHeight: 1.5 }}>
                                &ldquo;{t.centralIdea}&rdquo;
                              </p>
                            )}

                            {/* Badges for LOI, Key Concepts & ATL Skills */}
                            <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', paddingTop: '12px', borderTop: `1px solid ${borderColor}` }}>
                              <div style={{ fontSize: '11px', color: textSecondary }}>
                                <strong>LOIs:</strong> {linkedLois.length > 0 ? `${linkedLois.length} selected` : 'None linked'}
                              </div>
                              <div style={{ fontSize: '11px', color: textSecondary }}>
                                <strong>Key Concepts:</strong> {linkedKcs.length > 0 ? `${linkedKcs.length} selected` : 'None linked'}
                              </div>
                              <div style={{ fontSize: '11px', color: textSecondary }}>
                                <strong>ATL Skills:</strong> {linkedAtls.length > 0 ? `${linkedAtls.length} selected` : 'None linked'}
                              </div>
                            </div>
                          </div>

                          <div style={{ marginTop: '16px', paddingTop: '12px', borderTop: `1px solid ${borderColor}`, display: 'flex', justifyContent: 'flex-end', alignItems: 'center', fontSize: '12px', color: textSecondary }}>
                            <span style={{ fontWeight: 500, color: textPrimary, display: 'flex', alignItems: 'center', gap: '5px' }}>
                              <FontAwesomeIcon icon={faEdit} style={{ fontSize: '11px', color: textSecondary }} /> Click Card to Edit
                            </span>
                          </div>
                        </div>
                      )
                    })}
                  </div>
                )}
              </div>
            )}

          </div>
        )}

        {/* ----------------------------------------------------------        {/* ------------------------------------------------------------- */}
        {/* TAB 3: SUBJECT & STRAND ASSESSMENT (STREAMLINED MATRIX) */}
        {/* ------------------------------------------------------------- */}
        {activeTab === 'subject' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
                 {/* Database Setup Notice if table does not exist */}
            {dbAssessSetupNeeded && (
              <div 
                style={{ 
                  background: isDark ? 'rgba(245, 158, 11, 0.12)' : '#FBF3DB', 
                  border: `1px solid ${isDark ? 'rgba(245, 158, 11, 0.3)' : '#F5E6B8'}`,
                  borderRadius: '8px',
                  padding: '16px 20px'
                }}
              >
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', gap: '16px', flexWrap: 'wrap' }}>
                  <div style={{ display: 'flex', alignItems: 'flex-start', gap: '12px' }}>
                    <FontAwesomeIcon icon={faDatabase} style={{ color: isDark ? '#FBBF24' : '#956400', fontSize: '16px', marginTop: '2px' }} />
                    <div>
                      <h4 style={{ margin: '0 0 4px 0', fontSize: '13px', fontWeight: 600, color: isDark ? '#FBBF24' : '#956400' }}>
                        Database Setup Required: <code>pyp_strand_assessment</code>
                      </h4>
                      <p style={{ margin: 0, fontSize: '12px', color: isDark ? '#FDE68A' : '#785A00', lineHeight: 1.5 }}>
                        Tabel <code>pyp_strand_assessment</code> belum dibuat di Supabase. Silakan salin SQL di bawah ini dan jalankan di <strong>Supabase SQL Editor</strong> untuk mulai menyimpan penilaian strand siswa.
                      </p>
                    </div>
                  </div>
                  <button
                    type="button"
                    onClick={handleCopyAssessSql}
                    style={{
                      background: isDark ? '#FFFFFF' : '#18181B',
                      border: 'none',
                      color: isDark ? '#09090B' : '#FFFFFF',
                      borderRadius: '6px',
                      padding: '6px 14px',
                      fontSize: '12px',
                      fontWeight: 600,
                      cursor: 'pointer',
                      display: 'inline-flex',
                      alignItems: 'center',
                      gap: '6px',
                      transition: 'all 0.15s ease'
                    }}
                  >
                    <FontAwesomeIcon icon={copiedAssessSql ? faCheck : faCopy} style={{ fontSize: '11px' }} />
                    <span>{copiedAssessSql ? 'Copied' : 'Copy SQL Schema'}</span>
                  </button>
                </div>
              </div>
            )}

            {/* Subject Assessment Control Toolbar */}
            <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '24px' }}>
              <div style={{ marginBottom: '16px' }}>
                <h3 style={{ fontSize: '15px', fontWeight: 600, margin: '0 0 4px 0', color: textPrimary, letterSpacing: '-0.01em', display: 'flex', alignItems: 'center', gap: '8px' }}>
                  <FontAwesomeIcon icon={faListCheck} style={{ color: textSecondary, fontSize: '14px' }} />
                  PYP Subject &amp; Strand Assessment Matrix
                </h3>
                <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                  Evaluate student learning progression against Subject Strands. Scroll horizontally to grade across all strands.
                </p>
              </div>

              {/* Selector Filters (Class, Subject, Active Semester, Search) */}
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '14px', paddingTop: '16px', borderTop: `1px solid ${borderColor}` }}>
                
                {/* 1. Class Selector */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    1. PYP Class ({pypClasses.length})
                  </Label>
                  <select
                    value={selectedClassId || ''}
                    onChange={(e) => setSelectedClassId(e.target.value)}
                    style={{ ...selectStyle, width: '100%', padding: '8px 12px', fontWeight: 600, cursor: 'pointer' }}
                  >
                    {pypClasses.length === 0 ? (
                      <option value="">No PYP classes available</option>
                    ) : (
                      pypClasses.map(cls => (
                        <option key={cls.kelas_id} value={cls.kelas_id.toString()}>
                          {cls.kelas_nama}
                        </option>
                      ))
                    )}
                  </select>
                </div>

                {/* 2. Subject Selector (Filtered strictly for selected class & year) */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: isDark ? '#60A5FA' : '#1F6C9F', textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    2. PYP Subject * ({availableSubjectsForCurrentClass.length})
                  </Label>
                  <select
                    value={subjectTabSubjectId || ''}
                    onChange={(e) => setSubjectTabSubjectId(e.target.value)}
                    style={{
                      ...selectStyle,
                      width: '100%',
                      padding: '8px 12px',
                      fontWeight: 700,
                      cursor: 'pointer',
                      borderColor: isDark ? '#3B82F6' : '#BFDBFE',
                      background: isDark ? 'rgba(59, 130, 246, 0.08)' : '#F0F7FF',
                      color: isDark ? '#93C5FD' : '#1E40AF'
                    }}
                  >
                    {availableSubjectsForCurrentClass.length === 0 ? (
                      <option value="">No subjects assigned to this class</option>
                    ) : (
                      availableSubjectsForCurrentClass.map(subj => {
                        const strandCount = masterPypStrands.filter(st => st.subject_id === subj.id).length
                        return (
                          <option key={subj.id} value={subj.id.toString()}>
                            {subj.name} {subj.code ? `(${subj.code})` : ''} • {strandCount} Strands
                          </option>
                        )
                      })
                    )}
                  </select>
                </div>

                {/* 3. Active Grading Period Switcher */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    3. Grading Period *
                  </Label>
                  <div style={{ display: 'flex', borderRadius: '6px', border: `1px solid ${borderColor}`, background: isDark ? '#1F1F23' : '#F7F6F3', padding: '2px', gap: '2px' }}>
                    <button
                      type="button"
                      onClick={() => setActiveGradingSemester('1')}
                      style={{
                        flex: 1,
                        padding: '6px 10px',
                        fontSize: '11px',
                        fontWeight: activeGradingSemester === '1' ? 700 : 500,
                        borderRadius: '4px',
                        border: 'none',
                        cursor: 'pointer',
                        background: activeGradingSemester === '1' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                        color: activeGradingSemester === '1' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                        transition: 'all 0.15s ease',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center'
                      }}
                    >
                      Semester 1
                    </button>
                    <button
                      type="button"
                      onClick={() => setActiveGradingSemester('2')}
                      style={{
                        flex: 1,
                        padding: '6px 10px',
                        fontSize: '11px',
                        fontWeight: activeGradingSemester === '2' ? 700 : 500,
                        borderRadius: '4px',
                        border: 'none',
                        cursor: 'pointer',
                        background: activeGradingSemester === '2' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                        color: activeGradingSemester === '2' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                        transition: 'all 0.15s ease',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center'
                      }}
                    >
                      Semester 2
                    </button>
                  </div>
                </div>

                {/* 4. Student Search */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    4. Search Student ({filteredClassStudents.length}/{classStudents.length})
                  </Label>
                  <input
                    type="text"
                    placeholder="Search student by name..."
                    value={studentSearchQuery}
                    onChange={(e) => setStudentSearchQuery(e.target.value)}
                    style={{ ...inputStyle, width: '100%', padding: '8px 12px', fontSize: '12px' }}
                  />
                </div>

              </div>
            </div>

            {/* Assessment Content Section */}
            {availableSubjectsForCurrentClass.length === 0 ? (
              <div style={{ background: cardBg, border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '40px 20px', textAlign: 'center' }}>
                <FontAwesomeIcon icon={faBookOpen} style={{ fontSize: '24px', color: textSecondary, opacity: 0.5, marginBottom: '12px' }} />
                <h4 style={{ fontSize: '14px', fontWeight: 600, color: textPrimary, margin: '0 0 6px 0' }}>
                  No Subjects Linked to {currentSelectedClassObj?.kelas_nama || 'this class'}
                </h4>
                <p style={{ fontSize: '12px', color: textSecondary, margin: '0 0 16px 0', maxWidth: '480px', marginInline: 'auto' }}>
                  Go to <strong>/data/pyp-subject</strong> to create subjects and assign them to {currentSelectedClassObj?.kelas_nama || 'this class'}.
                </p>
                <Link
                  href="/data/pyp-subject"
                  style={{
                    display: 'inline-flex',
                    alignItems: 'center',
                    gap: '6px',
                    padding: '8px 16px',
                    borderRadius: '6px',
                    background: isDark ? '#FFFFFF' : '#18181B',
                    color: isDark ? '#09090B' : '#FFFFFF',
                    fontSize: '12px',
                    fontWeight: 600,
                    textDecoration: 'none'
                  }}
                >
                  <FontAwesomeIcon icon={faPlus} style={{ fontSize: '11px' }} />
                  Configure PYP Subjects
                </Link>
              </div>
            ) : currentSubjectStrands.length === 0 ? (
              <div style={{ background: cardBg, border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '40px 20px', textAlign: 'center' }}>
                <FontAwesomeIcon icon={faListCheck} style={{ fontSize: '24px', color: textSecondary, opacity: 0.5, marginBottom: '12px' }} />
                <h4 style={{ fontSize: '14px', fontWeight: 600, color: textPrimary, margin: '0 0 6px 0' }}>
                  No Strands Found for "{currentSelectedSubjectObj?.name}"
                </h4>
                <p style={{ fontSize: '12px', color: textSecondary, margin: '0 0 16px 0', maxWidth: '480px', marginInline: 'auto' }}>
                  This subject does not have learning strands configured yet. Add strands in <strong>/data/pyp-subject</strong>.
                </p>
                <Link
                  href="/data/pyp-subject"
                  style={{
                    display: 'inline-flex',
                    alignItems: 'center',
                    gap: '6px',
                    padding: '8px 16px',
                    borderRadius: '6px',
                    background: isDark ? '#FFFFFF' : '#18181B',
                    color: isDark ? '#09090B' : '#FFFFFF',
                    fontSize: '12px',
                    fontWeight: 600,
                    textDecoration: 'none'
                  }}
                >
                  <FontAwesomeIcon icon={faPlus} style={{ fontSize: '11px' }} />
                  Add Strands in /data/pyp-subject
                </Link>
              </div>
            ) : classStudents.length === 0 ? (
              <div style={{ background: cardBg, border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '40px 20px', textAlign: 'center' }}>
                <FontAwesomeIcon icon={faUserGraduate} style={{ fontSize: '24px', color: textSecondary, opacity: 0.5, marginBottom: '12px' }} />
                <h4 style={{ fontSize: '14px', fontWeight: 600, color: textPrimary, margin: '0 0 6px 0' }}>
                  No Students Registered in {currentSelectedClassObj?.kelas_nama || 'this class'}
                </h4>
                <p style={{ fontSize: '12px', color: textSecondary, margin: 0 }}>
                  Assign students to this class in <strong>/data/class</strong> to begin scoring subject strands.
                </p>
              </div>
            ) : (
              /* ── STREAMLINED MATRIX TABLE WITH FREEZE/STICKY COLUMNS ───────── */
              <div 
                style={{ 
                  background: cardBg, 
                  border: `1px solid ${borderColor}`, 
                  borderRadius: '8px', 
                  overflow: 'hidden',
                  display: 'flex',
                  flexDirection: 'column'
                }}
              >
                {/* Scrollable Matrix Table Container */}
                <div style={{ overflowX: 'auto', maxHeight: '72vh', overflowY: 'auto', position: 'relative' }}>
                  <table style={{ width: '100%', borderCollapse: 'separate', borderSpacing: 0, fontSize: '12px' }}>
                    <thead>
                      <tr style={{ background: isDark ? '#18181B' : '#F4F4F5' }}>
                        
                        {/* Sticky Column 1: # Index (Freeze Left: 0) */}
                        <th 
                          style={{ 
                            position: 'sticky', 
                            left: 0, 
                            top: 0, 
                            zIndex: 30, 
                            background: isDark ? '#18181B' : '#F4F4F5', 
                            padding: '10px 12px', 
                            textAlign: 'center', 
                            fontWeight: 700, 
                            color: textPrimary, 
                            width: '44px',
                            minWidth: '44px',
                            verticalAlign: 'top',
                            borderBottom: `2px solid ${borderColor}`,
                            borderRight: `1px solid ${borderColor}`
                          }}
                        >
                          <div style={{ height: '96px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                            <span style={{ fontSize: '11px', fontWeight: 700, fontFamily: 'monospace' }}>#</span>
                          </div>
                        </th>

                        {/* Sticky Column 2: Student Name (Freeze Left: 44px) */}
                        <th 
                          style={{ 
                            position: 'sticky', 
                            left: '44px', 
                            top: 0, 
                            zIndex: 30, 
                            background: isDark ? '#18181B' : '#F4F4F5', 
                            padding: '10px 14px', 
                            textAlign: 'left', 
                            fontWeight: 700, 
                            color: textPrimary, 
                            minWidth: '200px',
                            maxWidth: '240px',
                            verticalAlign: 'top',
                            borderBottom: `2px solid ${borderColor}`,
                            borderRight: `2px solid ${borderColor}`
                          }}
                        >
                          <div style={{ height: '96px', display: 'flex', flexDirection: 'column', justifyContent: 'center' }}>
                            <span style={{ fontSize: '12px', fontWeight: 700 }}>Student Name</span>
                            <span style={{ fontSize: '11px', color: textSecondary, fontWeight: 500, marginTop: '2px', fontFamily: 'monospace' }}>
                              {filteredClassStudents.length} Students
                            </span>
                          </div>
                        </th>

                        {/* Dynamic Strand Columns with Semester Applicability & Batch Fill */}
                        {currentSubjectStrands.map((strand, sIdx) => {
                          const strandScope = String(strand.semester || 'all')

                          return (
                            <th 
                              key={strand.id} 
                              style={{ 
                                position: 'sticky',
                                top: 0,
                                zIndex: 20,
                                background: isDark ? '#18181B' : '#F4F4F5',
                                padding: '10px 12px', 
                                textAlign: 'left', 
                                fontWeight: 700, 
                                color: textPrimary, 
                                minWidth: '210px',
                                maxWidth: '240px',
                                verticalAlign: 'top',
                                borderBottom: `2px solid ${borderColor}`,
                                borderRight: `1px solid ${borderColor}`
                              }}
                            >
                              <div style={{ height: '96px', display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}>
                                
                                {/* Row 1: Strand Title */}
                                <div 
                                  style={{ 
                                    height: '34px', 
                                    display: 'flex', 
                                    alignItems: 'flex-start', 
                                    justifyContent: 'space-between', 
                                    gap: '6px' 
                                  }}
                                  title={strand.description ? `${strand.name}\n\n${strand.description}` : strand.name}
                                >
                                  <span 
                                    style={{ 
                                      fontSize: '11px', 
                                      fontWeight: 600, 
                                      lineHeight: '1.3',
                                      display: '-webkit-box',
                                      WebkitLineClamp: 2,
                                      WebkitBoxOrient: 'vertical',
                                      overflow: 'hidden',
                                      textOverflow: 'ellipsis',
                                      color: textPrimary
                                    }}
                                  >
                                    {strand.name}
                                  </span>
                                  <span style={{ fontSize: '10px', fontFamily: 'monospace', fontWeight: 600, padding: '1px 4px', borderRadius: '3px', background: isDark ? '#27272A' : '#E4E4E7', color: textSecondary, flexShrink: 0 }}>
                                    #{sIdx + 1}
                                  </span>
                                </div>

                                {/* Row 2: Strand Scope */}
                                <div style={{ height: '24px', display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: '4px', borderTop: `1px solid ${borderColor}`, paddingTop: '3px' }}>
                                  <span style={{ fontSize: '9px', color: textSecondary, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em' }}>
                                    Scope:
                                  </span>
                                  <div style={{ display: 'inline-flex', borderRadius: '4px', border: `1px solid ${borderColor}`, overflow: 'hidden', background: isDark ? '#1F1F23' : '#F7F6F3', padding: '1px', gap: '1px' }}>
                                    <button
                                      type="button"
                                      title="Set strand scope to Semester 1 Only"
                                      onClick={() => handleRequestToggleStrandSemester(strand, '1')}
                                      style={{
                                        border: 'none',
                                        padding: '2px 5px',
                                        fontSize: '9px',
                                        fontWeight: strandScope === '1' ? 700 : 500,
                                        borderRadius: '3px',
                                        cursor: 'pointer',
                                        background: strandScope === '1' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                                        color: strandScope === '1' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                                        transition: 'all 0.15s ease'
                                      }}
                                    >
                                      Sem 1
                                    </button>
                                    <button
                                      type="button"
                                      title="Set strand scope to Semester 2 Only"
                                      onClick={() => handleRequestToggleStrandSemester(strand, '2')}
                                      style={{
                                        border: 'none',
                                        padding: '2px 5px',
                                        fontSize: '9px',
                                        fontWeight: strandScope === '2' ? 700 : 500,
                                        borderRadius: '3px',
                                        cursor: 'pointer',
                                        background: strandScope === '2' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                                        color: strandScope === '2' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                                        transition: 'all 0.15s ease'
                                      }}
                                    >
                                      Sem 2
                                    </button>
                                    <button
                                      type="button"
                                      title="Set strand scope to Both Semesters"
                                      onClick={() => handleRequestToggleStrandSemester(strand, 'all')}
                                      style={{
                                        border: 'none',
                                        padding: '2px 5px',
                                        fontSize: '9px',
                                        fontWeight: strandScope === 'all' ? 700 : 500,
                                        borderRadius: '3px',
                                        cursor: 'pointer',
                                        background: strandScope === 'all' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                                        color: strandScope === 'all' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                                        transition: 'all 0.15s ease'
                                      }}
                                    >
                                      Both
                                    </button>
                                  </div>
                                </div>

                                {/* Row 3: Column Quick Fill for Active Semester */}
                                <div style={{ height: '22px', display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: '4px' }}>
                                  <span style={{ fontSize: '9px', color: textSecondary, fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.04em' }}>
                                    Fill S{activeGradingSemester}:
                                  </span>
                                  <select
                                    defaultValue=""
                                    onChange={(e) => {
                                      if (e.target.value) {
                                        handleBatchSetStrandRatings(strand.id, e.target.value)
                                        e.target.value = ''
                                      }
                                    }}
                                    style={{
                                      ...selectStyle,
                                      padding: '1px 4px',
                                      fontSize: '9px',
                                      fontWeight: 600,
                                      cursor: 'pointer',
                                      height: '20px',
                                      borderRadius: '4px'
                                    }}
                                  >
                                    <option value="" disabled>Set all...</option>
                                    {STRAND_RATING_OPTIONS.map(opt => (
                                      <option key={opt.value} value={opt.value}>{opt.label}</option>
                                    ))}
                                  </select>
                                </div>

                              </div>
                            </th>
                          )
                        })}

                        {/* Status Column */}
                        <th 
                          style={{ 
                            position: 'sticky',
                            top: 0,
                            zIndex: 20,
                            background: isDark ? '#18181B' : '#F4F4F5',
                            padding: '10px 12px', 
                            textAlign: 'center', 
                            fontWeight: 700, 
                            color: textPrimary, 
                            width: '84px',
                            minWidth: '84px',
                            verticalAlign: 'top',
                            borderBottom: `2px solid ${borderColor}`
                          }}
                        >
                          <div style={{ height: '96px', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: '2px' }}>
                            <span style={{ fontSize: '11px', fontWeight: 700 }}>Status</span>
                            <span style={{ fontSize: '10px', color: textSecondary, fontWeight: 500, fontFamily: 'monospace' }}>
                              Sem {activeGradingSemester}
                            </span>
                          </div>
                        </th>
                      </tr>
                    </thead>
                    <tbody>
                      {filteredClassStudents.map((std, idx) => {
                        let assessedCount = 0
                        for (const st of currentSubjectStrands) {
                          const r = assessmentsMap[`${std.user_id}_${st.id}_${activeGradingSemester}`]
                          if (r && r !== 'N/A') assessedCount++
                        }
                        const isAllAssessed = currentSubjectStrands.length > 0 && assessedCount === currentSubjectStrands.length
                        const rowBg = idx % 2 === 0 ? (isDark ? '#141416' : '#FFFFFF') : (isDark ? '#18181B' : '#FAFAFA')

                        return (
                          <tr key={std.user_id} style={{ background: rowBg }}>
                            
                            {/* Sticky # Cell */}
                            <td 
                              style={{ 
                                position: 'sticky', 
                                left: 0, 
                                zIndex: 10, 
                                background: rowBg, 
                                padding: '8px 12px', 
                                color: textSecondary, 
                                fontFamily: 'monospace', 
                                fontSize: '11px',
                                textAlign: 'center',
                                borderBottom: `1px solid ${borderColor}`,
                                borderRight: `1px solid ${borderColor}`
                              }}
                            >
                              {idx + 1}
                            </td>

                            {/* Sticky Student Name Cell */}
                            <td 
                              style={{ 
                                position: 'sticky', 
                                left: '44px', 
                                zIndex: 10, 
                                background: rowBg, 
                                padding: '8px 14px', 
                                fontWeight: 600, 
                                color: textPrimary,
                                minWidth: '200px',
                                maxWidth: '240px',
                                whiteSpace: 'nowrap',
                                overflow: 'hidden',
                                textOverflow: 'ellipsis',
                                borderBottom: `1px solid ${borderColor}`,
                                borderRight: `2px solid ${borderColor}`
                              }}
                            >
                              {std.user_nama_depan} {std.user_nama_belakang}
                            </td>

                            {/* Strand Evaluation Dropdown Cells (Option 1: Active Semester + S1 Smart Reference) */}
                            {currentSubjectStrands.map(strand => {
                              const sem = Number(activeGradingSemester) || 1
                              const activeRating = assessmentsMap[`${std.user_id}_${strand.id}_${sem}`] || 'N/A'

                              let cellBg = isDark ? '#18181B' : '#FFFFFF'
                              let cellColor = textSecondary
                              let cellBorder = borderColor
                              if (activeRating === 'Beginning') {
                                cellBg = isDark ? 'rgba(245, 158, 11, 0.15)' : '#FBF3DB'
                                cellColor = isDark ? '#FBBF24' : '#956400'
                                cellBorder = isDark ? 'rgba(245, 158, 11, 0.3)' : '#F5E6B8'
                              } else if (activeRating === 'Developing') {
                                cellBg = isDark ? 'rgba(59, 130, 246, 0.15)' : '#E1F3FE'
                                cellColor = isDark ? '#93C5FD' : '#1F6C9F'
                                cellBorder = isDark ? 'rgba(59, 130, 246, 0.3)' : '#BFDBFE'
                              } else if (activeRating === 'Achieving') {
                                cellBg = isDark ? 'rgba(52, 211, 153, 0.15)' : '#EDF3EC'
                                cellColor = isDark ? '#34D399' : '#346538'
                                cellBorder = isDark ? 'rgba(52, 211, 153, 0.3)' : '#D1E7DD'
                              } else if (activeRating === 'Exceeding') {
                                cellBg = isDark ? 'rgba(168, 85, 247, 0.15)' : '#EDE9FE'
                                cellColor = isDark ? '#C084FC' : '#6D28D9'
                                cellBorder = isDark ? 'rgba(168, 85, 247, 0.3)' : '#DDD6FE'
                              }

                              return (
                                <td 
                                  key={strand.id} 
                                  style={{ 
                                    padding: '6px 8px',
                                    borderBottom: `1px solid ${borderColor}`,
                                    borderRight: `1px solid ${borderColor}`,
                                    verticalAlign: 'middle'
                                  }}
                                >
                                  <select
                                    value={activeRating}
                                    onChange={(e) => handleRatingChange(std.user_id, strand.id, e.target.value)}
                                    style={{
                                      ...selectStyle,
                                      width: '100%',
                                      padding: '5px 8px',
                                      fontSize: '11px',
                                      fontWeight: 600,
                                      cursor: 'pointer',
                                      background: cellBg,
                                      color: cellColor,
                                      border: `1px solid ${cellBorder}`,
                                      borderRadius: '4px'
                                    }}
                                  >
                                    {STRAND_RATING_OPTIONS.map(opt => (
                                      <option key={opt.value} value={opt.value}>
                                        {opt.label}
                                      </option>
                                    ))}
                                  </select>
                                </td>
                              )
                            })}

                            {/* Status Cell */}
                            <td 
                              style={{ 
                                padding: '8px 12px', 
                                textAlign: 'center',
                                borderBottom: `1px solid ${borderColor}`,
                                verticalAlign: 'middle'
                              }}
                            >
                              <span 
                                style={{
                                  fontSize: '10px',
                                  fontWeight: 600,
                                  textTransform: 'uppercase',
                                  letterSpacing: '0.04em',
                                  padding: '2px 8px',
                                  borderRadius: '9999px',
                                  display: 'inline-block',
                                  background: isAllAssessed 
                                    ? (isDark ? 'rgba(52, 211, 153, 0.15)' : '#EDF3EC') 
                                    : (isDark ? 'rgba(255,255,255,0.06)' : '#F4F4F5'),
                                  color: isAllAssessed 
                                    ? (isDark ? '#34D399' : '#346538') 
                                    : textSecondary,
                                  border: `1px solid ${
                                    isAllAssessed 
                                      ? (isDark ? 'rgba(52, 211, 153, 0.3)' : '#D1E7DD') 
                                      : borderColor
                                  }`
                                }}
                              >
                                {isAllAssessed ? 'Complete' : `${assessedCount}/${currentSubjectStrands.length}`}
                              </span>
                            </td>

                          </tr>
                        )
                      })}
                    </tbody>
                  </table>
                </div>
              </div>
            )}

          </div>
        )}

        {/* ------------------------------------------------------------- */}
        {/* TAB 4: TEACHER'S COMMENT */}
        {/* ------------------------------------------------------------- */}
        {activeTab === 'comment' && (
          <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
            
            {/* Database Setup Alert (If Table Doesn't Exist) */}
            {dbCommentSetupNeeded && (
              <div style={{
                background: isDark ? 'rgba(234, 88, 12, 0.12)' : '#FFF7ED',
                border: '1px solid #F97316',
                borderRadius: '10px',
                padding: '16px 20px',
                display: 'flex',
                alignItems: 'flex-start',
                justifyContent: 'space-between',
                flexWrap: 'wrap',
                gap: '14px'
              }}>
                <div style={{ display: 'flex', gap: '12px', alignItems: 'flex-start' }}>
                  <FontAwesomeIcon icon={faExclamationTriangle} style={{ color: '#F97316', fontSize: '18px', marginTop: '2px' }} />
                  <div>
                    <h4 style={{ margin: '0 0 4px 0', fontSize: '14px', fontWeight: 700, color: isDark ? '#FDBA74' : '#C2410C' }}>
                      Database Setup Required: <code>pyp_subject_comment</code>
                    </h4>
                    <p style={{ margin: 0, fontSize: '12px', color: isDark ? '#FED7AA' : '#9A3412', lineHeight: 1.5 }}>
                      Tabel <code>pyp_subject_comment</code> belum dibuat di Supabase. Salin skrip SQL di bawah ini dan jalankan di <strong>Supabase SQL Editor</strong> untuk mulai menyimpan komentar guru per semester.
                    </p>
                  </div>
                </div>

                <Button
                  onClick={handleCopyCommentSql}
                  style={{
                    background: '#F97316',
                    color: '#FFFFFF',
                    border: 'none',
                    borderRadius: '6px',
                    fontWeight: 600,
                    fontSize: '12px',
                    padding: '8px 16px',
                    display: 'flex',
                    alignItems: 'center',
                    gap: '6px',
                    cursor: 'pointer'
                  }}
                >
                  <FontAwesomeIcon icon={copiedCommentSql ? faCheck : faCopy} />
                  {copiedCommentSql ? 'Copied to Clipboard!' : 'Copy SQL Migration'}
                </Button>
              </div>
            )}

            {/* Main Container */}
            <div style={{ background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '24px' }}>
              
              {/* Header */}
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', flexWrap: 'wrap', gap: '16px', marginBottom: '20px' }}>
                <div>
                  <h3 style={{ fontSize: '15px', fontWeight: 600, margin: '0 0 4px 0', color: textPrimary, letterSpacing: '-0.01em', display: 'flex', alignItems: 'center', gap: '8px' }}>
                    <FontAwesomeIcon icon={faComments} style={{ color: textSecondary, fontSize: '14px' }} />
                    PYP Subject Teacher Comments
                  </h3>
                  <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                    Formulate structured transdisciplinary learning reflection comments for student report cards.
                  </p>
                </div>

                {/* Save All Comments Button */}
                <Button
                  onClick={handleSaveStudentComments}
                  disabled={savingComments || availableSubjectsForCurrentClass.length === 0}
                  style={{
                    background: isDark ? '#FFFFFF' : '#18181B',
                    color: isDark ? '#09090B' : '#FFFFFF',
                    border: 'none',
                    borderRadius: '6px',
                    fontWeight: 600,
                    fontSize: '12px',
                    padding: '8px 16px',
                    display: 'flex',
                    alignItems: 'center',
                    gap: '8px',
                    cursor: savingComments ? 'not-allowed' : 'pointer',
                    opacity: savingComments ? 0.7 : 1,
                    transition: 'all 0.15s ease'
                  }}
                >
                  <FontAwesomeIcon icon={savingComments ? faSpinner : faSave} spin={savingComments} style={{ fontSize: '12px' }} />
                  <span>{savingComments ? 'Saving...' : `Save Semester ${activeGradingSemester} Comments`}</span>
                </Button>
              </div>

              {/* Toolbar Filters (Class, Subject, Grading Period, Search Student) - IDENTICAL TO TAB 3 */}
              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '14px', paddingTop: '16px', borderTop: `1px solid ${borderColor}` }}>
                
                {/* 1. Class Selector */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    1. PYP Class ({pypClasses.length})
                  </Label>
                  <select
                    value={selectedClassId || ''}
                    onChange={(e) => setSelectedClassId(e.target.value)}
                    style={{ ...selectStyle, width: '100%', padding: '8px 12px', fontWeight: 600, cursor: 'pointer' }}
                  >
                    {pypClasses.length === 0 ? (
                      <option value="">No PYP classes available</option>
                    ) : (
                      pypClasses.map(cls => (
                        <option key={cls.kelas_id} value={cls.kelas_id.toString()}>
                          {cls.kelas_nama}
                        </option>
                      ))
                    )}
                  </select>
                </div>

                {/* 2. Subject Selector */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: isDark ? '#60A5FA' : '#1F6C9F', textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    2. PYP Subject * ({availableSubjectsForCurrentClass.length})
                  </Label>
                  <select
                    value={subjectTabSubjectId || ''}
                    onChange={(e) => setSubjectTabSubjectId(e.target.value)}
                    style={{
                      ...selectStyle,
                      width: '100%',
                      padding: '8px 12px',
                      fontWeight: 700,
                      cursor: 'pointer',
                      borderColor: isDark ? '#3B82F6' : '#BFDBFE',
                      background: isDark ? 'rgba(59, 130, 246, 0.08)' : '#F0F7FF',
                      color: isDark ? '#93C5FD' : '#1E40AF'
                    }}
                  >
                    {availableSubjectsForCurrentClass.length === 0 ? (
                      <option value="">No subjects assigned to this class</option>
                    ) : (
                      availableSubjectsForCurrentClass.map(subj => (
                        <option key={subj.id} value={subj.id.toString()}>
                          {subj.name} {subj.code ? `(${subj.code})` : ''}
                        </option>
                      ))
                    )}
                  </select>
                </div>

                {/* 3. Active Grading Period Switcher */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    3. Grading Period *
                  </Label>
                  <div style={{ display: 'flex', borderRadius: '6px', border: `1px solid ${borderColor}`, background: isDark ? '#1F1F23' : '#F7F6F3', padding: '2px', gap: '2px' }}>
                    <button
                      type="button"
                      onClick={() => setActiveGradingSemester('1')}
                      style={{
                        flex: 1,
                        padding: '6px 10px',
                        fontSize: '11px',
                        fontWeight: activeGradingSemester === '1' ? 700 : 500,
                        borderRadius: '4px',
                        border: 'none',
                        cursor: 'pointer',
                        background: activeGradingSemester === '1' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                        color: activeGradingSemester === '1' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                        transition: 'all 0.15s ease',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center'
                      }}
                    >
                      Semester 1
                    </button>
                    <button
                      type="button"
                      onClick={() => setActiveGradingSemester('2')}
                      style={{
                        flex: 1,
                        padding: '6px 10px',
                        fontSize: '11px',
                        fontWeight: activeGradingSemester === '2' ? 700 : 500,
                        borderRadius: '4px',
                        border: 'none',
                        cursor: 'pointer',
                        background: activeGradingSemester === '2' ? (isDark ? '#FFFFFF' : '#18181B') : 'transparent',
                        color: activeGradingSemester === '2' ? (isDark ? '#09090B' : '#FFFFFF') : textSecondary,
                        transition: 'all 0.15s ease',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center'
                      }}
                    >
                      Semester 2
                    </button>
                  </div>
                </div>

                {/* 4. Student Search */}
                <div>
                  <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                    4. Search Student ({filteredClassStudents.length}/{classStudents.length})
                  </Label>
                  <input
                    type="text"
                    placeholder="Search student by name..."
                    value={studentSearchQuery}
                    onChange={(e) => setStudentSearchQuery(e.target.value)}
                    style={{ ...inputStyle, width: '100%', padding: '8px 12px', fontSize: '12px' }}
                  />
                </div>

              </div>
            </div>

            {/* Context Summary & Completion Metric */}
            {availableSubjectsForCurrentClass.length > 0 && currentSelectedSubjectObj && (
              <div style={{
                background: isDark ? '#1F1F23' : '#FBFBFA',
                border: `1px solid ${borderColor}`,
                borderRadius: '8px',
                padding: '12px 18px',
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                flexWrap: 'wrap',
                gap: '12px'
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '8px', flexWrap: 'wrap', fontSize: '13px' }}>
                  <span style={{ fontWeight: 600, color: textPrimary }}>
                    {currentSelectedSubjectObj.name}
                  </span>
                  {currentSelectedSubjectObj.code && (
                    <span style={{
                      fontSize: '11px',
                      fontWeight: 600,
                      padding: '2px 6px',
                      borderRadius: '4px',
                      background: isDark ? 'rgba(255,255,255,0.06)' : '#F4F4F5',
                      color: textSecondary,
                      fontFamily: 'monospace'
                    }}>
                      {currentSelectedSubjectObj.code}
                    </span>
                  )}
                  <span style={{ color: textSecondary }}>•</span>
                  <span style={{ color: textSecondary }}>
                    Class: <strong style={{ color: textPrimary }}>{currentSelectedClassObj?.kelas_nama}</strong>
                  </span>
                  <span style={{ color: textSecondary }}>•</span>
                  <span style={{
                    fontSize: '11px',
                    fontWeight: 600,
                    padding: '2px 6px',
                    borderRadius: '4px',
                    background: isDark ? 'rgba(255,255,255,0.06)' : '#F4F4F5',
                    color: textPrimary
                  }}>
                    Semester {activeGradingSemester}
                  </span>
                </div>

                {/* Completion Metric */}
                {(() => {
                  const filledCount = classStudents.filter(st => (studentCommentsMap[st.user_id] || '').trim().length > 0).length
                  const pct = classStudents.length > 0 ? Math.round((filledCount / classStudents.length) * 100) : 0
                  return (
                    <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                      <span style={{ fontSize: '11px', color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', fontWeight: 600 }}>
                        Progress:
                      </span>
                      <span style={{
                        fontSize: '11px',
                        fontWeight: 600,
                        padding: '2px 8px',
                        borderRadius: '9999px',
                        background: filledCount === classStudents.length && classStudents.length > 0
                          ? (isDark ? 'rgba(52, 211, 153, 0.15)' : '#EDF3EC')
                          : (isDark ? 'rgba(255,255,255,0.06)' : '#F4F4F5'),
                        color: filledCount === classStudents.length && classStudents.length > 0
                          ? (isDark ? '#34D399' : '#346538')
                          : textSecondary,
                        border: `1px solid ${filledCount === classStudents.length && classStudents.length > 0 ? (isDark ? 'rgba(52, 211, 153, 0.3)' : '#D1E7DD') : borderColor}`
                      }}>
                        {filledCount}/{classStudents.length} Completed ({pct}%)
                      </span>
                    </div>
                  )
                })()}
              </div>
            )}

            {/* Students Comments List */}
            {availableSubjectsForCurrentClass.length === 0 ? (
              <div style={{ background: cardBg, border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '40px 20px', textAlign: 'center' }}>
                <FontAwesomeIcon icon={faInfoCircle} style={{ fontSize: '20px', color: textSecondary, marginBottom: '8px' }} />
                <p style={{ margin: 0, fontSize: '13px', fontWeight: 600, color: textPrimary }}>
                  No subjects assigned to {currentSelectedClassObj?.kelas_nama || 'this class'}.
                </p>
                <p style={{ margin: '4px 0 0 0', fontSize: '12px', color: textSecondary }}>
                  Assign subjects to this class in <Link href="/data/pyp-subject" style={{ color: textPrimary, textDecoration: 'underline' }}>PYP Subjects Configuration</Link>.
                </p>
              </div>
            ) : filteredClassStudents.length === 0 ? (
              <div style={{ background: cardBg, border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '40px 20px', textAlign: 'center' }}>
                <FontAwesomeIcon icon={faInbox} style={{ fontSize: '20px', color: textSecondary, marginBottom: '8px' }} />
                <p style={{ margin: 0, fontSize: '13px', fontWeight: 600, color: textPrimary }}>
                  No students found in {currentSelectedClassObj?.kelas_nama || 'this class'}.
                </p>
              </div>
            ) : (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '14px' }}>
                {filteredClassStudents.map((std, idx) => {
                  const studentName = `${std.user_nama_depan || ''} ${std.user_nama_belakang || ''}`.trim() || 'Student'
                  const currentComment = studentCommentsMap[std.user_id] || ''
                  const originalComment = originalCommentsMap[std.user_id] || ''
                  const isDirty = currentComment !== originalComment
                  const charCount = currentComment.length
                  const wordCount = currentComment.trim() ? currentComment.trim().split(/\s+/).length : 0

                  return (
                    <div
                      key={std.user_id}
                      style={{
                        background: cardBg,
                        border: `1px solid ${isDirty ? (isDark ? '#FBBF24' : '#EAB308') : borderColor}`,
                        borderRadius: '8px',
                        padding: '16px 18px',
                        display: 'flex',
                        flexDirection: 'column',
                        gap: '10px',
                        transition: 'all 0.15s ease'
                      }}
                    >
                      {/* Student Info Bar */}
                      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '8px' }}>
                        
                        <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
                          {/* Student Avatar / Initials */}
                          <div style={{
                            width: '30px',
                            height: '30px',
                            borderRadius: '4px',
                            background: isDark ? 'rgba(255,255,255,0.06)' : '#F4F4F5',
                            color: textPrimary,
                            border: `1px solid ${borderColor}`,
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'center',
                            fontSize: '11px',
                            fontWeight: 700,
                            fontFamily: 'monospace'
                          }}>
                            {(std.user_nama_depan?.[0] || 'S').toUpperCase()}
                          </div>

                          <div style={{ display: 'flex', alignItems: 'center', gap: '8px', flexWrap: 'wrap' }}>
                            <span style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, fontFamily: 'monospace' }}>
                              #{idx + 1}
                            </span>
                            <span style={{ fontSize: '13px', fontWeight: 600, color: textPrimary }}>
                              {studentName}
                            </span>
                          </div>
                        </div>

                        {/* Status Badges (Minimalist Pastels) */}
                        <div style={{ display: 'flex', alignItems: 'center', gap: '6px' }}>
                          {isDirty && (
                            <span style={{
                              fontSize: '10px',
                              fontWeight: 600,
                              textTransform: 'uppercase',
                              letterSpacing: '0.05em',
                              padding: '2px 6px',
                              borderRadius: '4px',
                              background: isDark ? 'rgba(245, 158, 11, 0.15)' : '#FBF3DB',
                              color: isDark ? '#FBBF24' : '#956400',
                              border: `1px solid ${isDark ? 'rgba(245, 158, 11, 0.3)' : '#F5E6B8'}`
                            }}>
                              Unsaved
                            </span>
                          )}

                          <span style={{
                            fontSize: '10px',
                            fontWeight: 600,
                            textTransform: 'uppercase',
                            letterSpacing: '0.05em',
                            padding: '2px 6px',
                            borderRadius: '4px',
                            background: currentComment.trim().length > 0 
                              ? (isDark ? 'rgba(52, 211, 153, 0.12)' : '#EDF3EC') 
                              : (isDark ? 'rgba(255,255,255,0.04)' : '#F4F4F5'),
                            color: currentComment.trim().length > 0 
                              ? (isDark ? '#34D399' : '#346538') 
                              : textSecondary,
                            border: `1px solid ${currentComment.trim().length > 0 ? (isDark ? 'rgba(52, 211, 153, 0.25)' : '#D1E7DD') : borderColor}`
                          }}>
                            {currentComment.trim().length > 0 ? 'Completed' : 'Pending'}
                          </span>
                        </div>

                      </div>

                      {/* Comment Textarea (Clean Minimalist Document Style) */}
                      <div>
                        <textarea
                          rows={3}
                          value={currentComment}
                          onChange={(e) => {
                            const val = e.target.value
                            setStudentCommentsMap(prev => ({
                              ...prev,
                              [std.user_id]: val
                            }))
                          }}
                          placeholder={`Write Semester ${activeGradingSemester} reflection comment for ${studentName}...`}
                          style={{
                            ...inputStyle,
                            width: '100%',
                            padding: '10px 12px',
                            fontSize: '13px',
                            lineHeight: 1.6,
                            resize: 'vertical',
                            minHeight: '76px',
                            fontFamily: 'inherit',
                            background: isDark ? '#18181B' : '#FFFFFF',
                            borderRadius: '6px'
                          }}
                        />
                      </div>

                      {/* Footer Info: Word/Char count */}
                      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', fontSize: '11px', color: textSecondary, fontFamily: 'monospace' }}>
                        <span>
                          {wordCount} words • {charCount} chars
                        </span>
                        {isDirty && (
                          <span style={{ color: isDark ? '#FBBF24' : '#D97706' }}>
                            Unsaved changes
                          </span>
                        )}
                      </div>

                    </div>
                  )
                })}
              </div>
            )}

          </div>
        )}

      </div>

      {/* ------------------------------------------------------------- */}
      {/* 4-STEP WIZARD MODAL: CREATE UNIT FOR PYP CLASS */}
      {/* ------------------------------------------------------------- */}
      {showClassUnitModal && (
        <Modal
          isOpen={showClassUnitModal}
          onClose={() => setShowClassUnitModal(false)}
          disableBackdropClose={true}
          title={editingUnit ? `Edit Unit: ${editingUnit.title}` : `Create Unit for ${currentSelectedClassObj?.kelas_nama || 'PYP Class'}`}
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: '20px' }}>
            
            {/* Interactive Step Progress Tabs */}
            <div style={{ display: 'flex', gap: '8px', borderBottom: `1px solid ${borderColor}`, paddingBottom: '8px' }}>
              {[
                { step: 1, label: '1. Unit Basics' },
                { step: 2, label: `2. Lines of Inquiry (${selectedLoiIds.length})` },
                { step: 3, label: `3. Key Concepts (${selectedKcIds.length})` },
                { step: 4, label: `4. ATL Skills (${selectedAtlIds.length})` }
              ].map(tab => {
                const isActive = wizardStep === tab.step
                return (
                  <button
                    key={tab.step}
                    type="button"
                    onClick={() => setWizardStep(tab.step)}
                    style={{
                      flex: 1,
                      background: 'none',
                      border: 'none',
                      borderBottom: isActive ? `2px solid ${textPrimary}` : '2px solid transparent',
                      paddingBottom: '8px',
                      fontSize: '12px',
                      fontWeight: isActive ? 700 : 500,
                      color: isActive ? textPrimary : textSecondary,
                      cursor: 'pointer',
                      textAlign: 'center',
                      transition: 'all 0.15s ease'
                    }}
                    onMouseEnter={e => {
                      if (!isActive) e.currentTarget.style.color = textPrimary
                    }}
                    onMouseLeave={e => {
                      if (!isActive) e.currentTarget.style.color = textSecondary
                    }}
                  >
                    {tab.label}
                  </button>
                )
              })}
            </div>

            {/* STEP 1: UNIT BASICS */}
            {wizardStep === 1 && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
                <div>
                  <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                    Unit Title *
                  </Label>
                  <Input
                    type="text"
                    required
                    placeholder="e.g. How We Express Identity Through Art"
                    value={unitFormData.title}
                    onChange={e => setUnitFormData({ ...unitFormData, title: e.target.value })}
                    style={inputStyle}
                  />
                </div>

                <div>
                  <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                    Central Idea *
                  </Label>

                  {selectedCiObj ? (
                    <div style={{ background: isDark ? '#27272A' : '#F4F4F5', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '12px 14px', display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: '12px' }}>
                      <div>
                        <div style={{ fontSize: '11px', fontWeight: 600, color: isDark ? '#60A5FA' : '#1F6C9F', textTransform: 'uppercase', letterSpacing: '0.05em', marginBottom: '4px' }}>
                          Selected Central Idea
                        </div>
                        <div style={{ fontSize: '13px', fontWeight: 600, color: textPrimary, lineHeight: 1.4 }}>
                          &ldquo;{selectedCiObj.name}&rdquo;
                        </div>
                      </div>
                      <Button
                        type="button"
                        onClick={() => {
                          setSelectedCiObj(null)
                          setUnitFormData(prev => ({ ...prev, centralIdea: '' }))
                          setSearchCiQuery('')
                          setShowCiSuggestions(true)
                        }}
                        style={{ background: cardBg, border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '12px', padding: '6px 12px', whiteSpace: 'nowrap' }}
                      >
                        Change
                      </Button>
                    </div>
                  ) : (
                    <div style={{ position: 'relative' }}>
                      <Input
                        type="text"
                        placeholder="Search Central Idea (A-Z)..."
                        value={searchCiQuery}
                        onFocus={() => setShowCiSuggestions(true)}
                        onBlur={() => setTimeout(() => setShowCiSuggestions(false), 200)}
                        onChange={e => {
                          setSearchCiQuery(e.target.value)
                          setShowCiSuggestions(true)
                        }}
                        style={inputStyle}
                      />

                      {/* Searchable Autocomplete Suggestions Popup (Sorted A-Z) */}
                      {showCiSuggestions && filteredCiSuggestions.length > 0 && (
                        <div
                          style={{
                            position: 'absolute',
                            top: '100%',
                            left: 0,
                            right: 0,
                            zIndex: 50,
                            marginTop: '4px',
                            maxHeight: '220px',
                            overflowY: 'auto',
                            background: cardBg,
                            border: `1px solid ${borderColor}`,
                            borderRadius: '8px',
                            boxShadow: '0 4px 12px rgba(0,0,0,0.15)',
                            padding: '4px'
                          }}
                        >
                          <div style={{ padding: '6px 10px', fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: `1px solid ${borderColor}` }}>
                            Central Ideas A-Z ({filteredCiSuggestions.length})
                          </div>
                          {filteredCiSuggestions.map((ci) => (
                            <div
                              key={ci.id}
                              onMouseDown={(e) => {
                                e.preventDefault()
                                setSelectedCiObj(ci)
                                setUnitFormData(prev => ({ ...prev, centralIdea: ci.name }))
                                setShowCiSuggestions(false)
                              }}
                              style={{
                                padding: '10px 12px',
                                fontSize: '12px',
                                color: textPrimary,
                                cursor: 'pointer',
                                borderRadius: '6px',
                                transition: 'background 0.1s ease',
                                lineHeight: 1.4,
                                borderBottom: `1px solid ${borderColor}`
                              }}
                              onMouseEnter={(e) => {
                                e.currentTarget.style.background = isDark ? '#27272A' : '#F4F4F5'
                              }}
                              onMouseLeave={(e) => {
                                e.currentTarget.style.background = 'transparent'
                              }}
                            >
                              {ci.name}
                            </div>
                          ))}
                        </div>
                      )}

                      {showCiSuggestions && filteredCiSuggestions.length === 0 && (
                        <div style={{ position: 'absolute', top: '100%', left: 0, right: 0, zIndex: 50, marginTop: '4px', background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '12px', textAlign: 'center', fontSize: '12px', color: textSecondary }}>
                          No matching Central Idea for &ldquo;{searchCiQuery}&rdquo;.
                        </div>
                      )}
                    </div>
                  )}
                </div>

                <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
                  <div>
                    <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                      Transdisciplinary Theme
                    </Label>
                    <select
                      value={unitFormData.theme}
                      onChange={e => setUnitFormData({ ...unitFormData, theme: e.target.value })}
                      style={{ ...selectStyle, width: '100%' }}
                    >
                      {TRANSDISCIPLINARY_THEMES.map(th => (
                        <option key={th} value={th}>{th}</option>
                      ))}
                    </select>
                  </div>

                  <div>
                    <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                      Duration (Weeks)
                    </Label>
                    <Input
                      type="number"
                      min="1"
                      max="12"
                      value={unitFormData.duration}
                      onChange={e => setUnitFormData({ ...unitFormData, duration: e.target.value })}
                      style={inputStyle}
                    />
                  </div>
                </div>
              </div>
            )}

            {/* STEP 2: LINES OF INQUIRY (Multi-Select Autocomplete A-Z) */}
            {wizardStep === 2 && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '14px', paddingBottom: '20px' }}>
                <Label style={{ fontSize: '12px', fontWeight: 600, color: textPrimary }}>
                  Lines of Inquiry * <span style={{ fontSize: '11px', fontWeight: 400, color: textSecondary }}>({selectedLoiObjs.length} Selected)</span>
                </Label>

                {/* Selected LOI Badges / Chips Container */}
                {selectedLoiObjs.length > 0 && (
                  <div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px', background: isDark ? '#27272A' : '#F4F4F5', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '10px 12px' }}>
                    {selectedLoiObjs.map(loi => (
                      <div
                        key={loi.id}
                        style={{
                          display: 'inline-flex',
                          alignItems: 'center',
                          gap: '8px',
                          background: cardBg,
                          border: `1px solid ${borderColor}`,
                          borderRadius: '6px',
                          padding: '6px 10px',
                          fontSize: '12px',
                          color: textPrimary,
                          lineHeight: 1.4
                        }}
                      >
                        <span>{loi.name}</span>
                        <button
                          type="button"
                          onClick={() => {
                            setSelectedLoiObjs(prev => prev.filter(item => item.id !== loi.id))
                            setSelectedLoiIds(prev => prev.filter(id => id !== loi.id))
                          }}
                          style={{
                            background: 'none',
                            border: 'none',
                            color: textSecondary,
                            cursor: 'pointer',
                            padding: '0 2px',
                            fontSize: '12px',
                            lineHeight: 1
                          }}
                        >
                          ✕
                        </button>
                      </div>
                    ))}
                  </div>
                )}

                {/* Search Box Input */}
                <div style={{ position: 'relative' }}>
                  <Input
                    type="text"
                    placeholder="Search Lines of Inquiry (A-Z)..."
                    value={searchLoiQuery}
                    onFocus={() => setShowLoiSuggestions(true)}
                    onBlur={() => setTimeout(() => setShowLoiSuggestions(false), 200)}
                    onChange={e => {
                      setSearchLoiQuery(e.target.value)
                      setShowLoiSuggestions(true)
                    }}
                    style={inputStyle}
                  />

                  {/* Multi-Select Autocomplete Suggestions Popup */}
                  {showLoiSuggestions && filteredLoiSuggestions.length > 0 && (
                    <div
                      style={{
                        position: 'absolute',
                        top: '100%',
                        left: 0,
                        right: 0,
                        zIndex: 50,
                        marginTop: '4px',
                        maxHeight: '160px',
                        overflowY: 'auto',
                        background: cardBg,
                        border: `1px solid ${borderColor}`,
                        borderRadius: '8px',
                        boxShadow: '0 4px 12px rgba(0,0,0,0.15)',
                        padding: '4px'
                      }}
                    >
                      <div style={{ padding: '6px 10px', fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.05em', borderBottom: `1px solid ${borderColor}` }}>
                        Lines of Inquiry A-Z ({filteredLoiSuggestions.length})
                      </div>
                      {filteredLoiSuggestions.map((loi) => {
                        const isAlreadySelected = selectedLoiObjs.some(item => item.id === loi.id)
                        return (
                          <div
                            key={loi.id}
                            onMouseDown={(e) => {
                              e.preventDefault()
                              if (isAlreadySelected) {
                                setSelectedLoiObjs(prev => prev.filter(item => item.id !== loi.id))
                                setSelectedLoiIds(prev => prev.filter(id => id !== loi.id))
                              } else {
                                setSelectedLoiObjs(prev => [...prev, loi])
                                setSelectedLoiIds(prev => [...prev, loi.id])
                              }
                              // Auto-close popup and clear query so Next button is unblocked
                              setSearchLoiQuery('')
                              setShowLoiSuggestions(false)
                            }}
                            style={{
                              padding: '10px 12px',
                              fontSize: '12px',
                              color: isAlreadySelected ? (isDark ? '#60A5FA' : '#1F6C9F') : textPrimary,
                              fontWeight: isAlreadySelected ? 600 : 400,
                              cursor: 'pointer',
                              borderRadius: '6px',
                              transition: 'background 0.1s ease',
                              lineHeight: 1.4,
                              borderBottom: `1px solid ${borderColor}`,
                              display: 'flex',
                              justifyContent: 'space-between',
                              alignItems: 'center',
                              gap: '8px',
                              background: isAlreadySelected ? (isDark ? 'rgba(59, 130, 246, 0.1)' : '#E1F3FE') : 'transparent'
                            }}
                            onMouseEnter={(e) => {
                              if (!isAlreadySelected) e.currentTarget.style.background = isDark ? '#27272A' : '#F4F4F5'
                            }}
                            onMouseLeave={(e) => {
                              if (!isAlreadySelected) e.currentTarget.style.background = 'transparent'
                            }}
                          >
                            <span>{loi.name}</span>
                            {isAlreadySelected && (
                              <span style={{ fontSize: '11px', fontWeight: 700, color: isDark ? '#60A5FA' : '#1F6C9F' }}>
                                ✓ Added
                              </span>
                            )}
                          </div>
                        )
                      })}
                    </div>
                  )}

                  {showLoiSuggestions && filteredLoiSuggestions.length === 0 && (
                    <div style={{ position: 'absolute', top: '100%', left: 0, right: 0, zIndex: 50, marginTop: '4px', background: cardBg, border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '12px', textAlign: 'center', fontSize: '12px', color: textSecondary }}>
                      No matching Line of Inquiry for &ldquo;{searchLoiQuery}&rdquo;.
                    </div>
                  )}
                </div>
              </div>
            )}

            {/* STEP 3: KEY CONCEPTS */}
            {wizardStep === 3 && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '14px' }}>
                <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                  Select the Key Concepts for this Unit.
                </p>

                {kcList.length === 0 ? (
                  <div style={{ border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '24px', textAlign: 'center', color: textSecondary, fontSize: '13px' }}>
                    No Key Concepts found. You can add Key Concepts in Tab 1 (Master Templates).
                  </div>
                ) : (
                  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: '12px', maxHeight: '340px', overflowY: 'auto' }}>
                    {kcList.map(kc => {
                      const isChecked = selectedKcIds.includes(kc.id)
                      return (
                        <label
                          key={kc.id}
                          style={{
                            display: 'flex',
                            flexDirection: 'column',
                            gap: '4px',
                            background: isChecked ? (isDark ? 'rgba(59, 130, 246, 0.12)' : '#E1F3FE') : cardBg,
                            border: `1px solid ${isChecked ? (isDark ? '#60A5FA' : '#1F6C9F') : borderColor}`,
                            borderRadius: '8px',
                            padding: '12px 14px',
                            cursor: 'pointer',
                            transition: 'all 0.15s ease'
                          }}
                        >
                          <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
                            <input
                              type="checkbox"
                              checked={isChecked}
                              onChange={() => toggleKcSelection(kc.id)}
                            />
                            <span style={{ fontSize: '13px', fontWeight: 700, color: isChecked ? (isDark ? '#60A5FA' : '#1F6C9F') : textPrimary }}>
                              {kc.key}
                            </span>
                          </div>

                          {kc.question && (
                            <div style={{ fontSize: '12px', fontWeight: 600, color: textPrimary, marginLeft: '24px', lineHeight: 1.3 }}>
                              &ldquo;{kc.question}&rdquo;
                            </div>
                          )}

                          {kc.definition && (
                            <div style={{ fontSize: '11px', color: textSecondary, marginLeft: '24px', lineHeight: 1.4, marginTop: '2px' }}>
                              {kc.definition}
                            </div>
                          )}
                        </label>
                      )
                    })}
                  </div>
                )}
              </div>
            )}

            {/* STEP 4: ATL SKILLS & NOTES */}
            {wizardStep === 4 && (
              <div style={{ display: 'flex', flexDirection: 'column', gap: '14px' }}>
                <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
                  Select ATL Skills and add notes.
                </p>

                {atlsList.length === 0 ? (
                  <div style={{ border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '24px', textAlign: 'center', color: textSecondary, fontSize: '13px' }}>
                    No ATL Skills found. You can add ATL Skills in Tab 1 (Master Templates).
                  </div>
                ) : (
                  <div style={{ display: 'flex', flexDirection: 'column', gap: '10px', maxHeight: '300px', overflowY: 'auto', paddingRight: '4px' }}>
                    {atlsList.map(atl => {
                      const isChecked = selectedAtlIds.includes(atl.id)
                      return (
                        <div
                          key={atl.id}
                          style={{
                            display: 'flex',
                            flexDirection: 'column',
                            gap: '8px',
                            background: isChecked ? (isDark ? '#27272A' : '#F4F4F5') : cardBg,
                            border: `1px solid ${isChecked ? textPrimary : borderColor}`,
                            borderRadius: '6px',
                            padding: '12px'
                          }}
                        >
                          <label style={{ display: 'flex', alignItems: 'center', gap: '10px', cursor: 'pointer' }}>
                            <input
                              type="checkbox"
                              checked={isChecked}
                              onChange={() => toggleAtlSelection(atl.id)}
                            />
                            <span style={{ fontSize: '13px', fontWeight: 600, color: textPrimary }}>
                              {atl.name}
                            </span>
                          </label>

                          {isChecked && (
                            <div style={{ marginTop: '4px', marginLeft: '28px' }}>
                              <textarea
                                rows={3}
                                placeholder="Add specific ATL skill indicators / bullet points (e.g., • Ask questions to find information&#10;• Find information from different sources)..."
                                value={atlNotes[atl.id] || ''}
                                onChange={e => setAtlNotes({ ...atlNotes, [atl.id]: e.target.value })}
                                style={{
                                  width: '100%',
                                  background: isDark ? '#18181B' : '#FFFFFF',
                                  border: `1px solid ${borderColor}`,
                                  borderRadius: '6px',
                                  padding: '8px 10px',
                                  fontSize: '12px',
                                  color: textPrimary,
                                  fontFamily: 'inherit',
                                  lineHeight: 1.5,
                                  resize: 'vertical',
                                  outline: 'none'
                                }}
                              />
                            </div>
                          )}
                        </div>
                      )
                    })}
                  </div>
                )}
              </div>
            )}

            {/* Modal Controls Bar */}
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: '12px', paddingTop: '12px', borderTop: `1px solid ${borderColor}` }}>
              {wizardStep > 1 ? (
                <Button
                  type="button"
                  onClick={() => setWizardStep(prev => prev - 1)}
                  style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '13px' }}
                >
                  <FontAwesomeIcon icon={faChevronLeft} style={{ marginRight: '6px' }} /> Back
                </Button>
              ) : <div />}

              <div style={{ display: 'flex', gap: '8px' }}>
                <Button
                  type="button"
                  onClick={() => setShowClassUnitModal(false)}
                  style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '13px' }}
                >
                  Cancel
                </Button>

                {wizardStep < 4 ? (
                  <Button
                    type="button"
                    onClick={() => {
                      if (wizardStep === 1) {
                        if (!unitFormData.title.trim()) {
                          setNotif({ isOpen: true, title: 'Missing Unit Title', message: 'Please enter a Unit Title before proceeding.', type: 'error' })
                          return
                        }
                        if (!selectedCiObj) {
                          setNotif({ isOpen: true, title: 'Central Idea Required', message: 'Please select a Central Idea from the list.', type: 'error' })
                          return
                        }
                      }
                      setWizardStep(prev => prev + 1)
                    }}
                    style={{ background: textPrimary, color: isDark ? '#09090B' : '#FFFFFF', fontSize: '13px', fontWeight: 600 }}
                  >
                    Next <FontAwesomeIcon icon={faChevronRight} style={{ marginLeft: '6px' }} />
                  </Button>
                ) : (
                  <Button
                    type="button"
                    disabled={submittingUnit}
                    onClick={handleSaveClassUnitWithPivots}
                    style={{ background: textPrimary, color: isDark ? '#09090B' : '#FFFFFF', fontSize: '13px', fontWeight: 600 }}
                  >
                    {submittingUnit ? (editingUnit ? 'Updating Unit...' : 'Saving Unit...') : (editingUnit ? 'Save Changes' : 'Save Complete Unit')}
                  </Button>
                )}
              </div>
            </div>

          </div>
        </Modal>
      )}

      {/* ------------------------------------------------------------- */}
      {/* ADD / EDIT MASTER LIST ITEM MODAL */}
      {/* ------------------------------------------------------------- */}
      {showMasterModal && (
        <Modal
          isOpen={showMasterModal}
          onClose={() => setShowMasterModal(false)}
          disableBackdropClose={true}
          title={`${editingItem ? 'Edit' : 'Add New'} ${
            masterSubTab === 'ci' ? 'Central Idea' :
            masterSubTab === 'loi' ? 'Line of Inquiry' :
            masterSubTab === 'atls' ? 'ATL Skill' : 'Key Concept'
          }`}
        >
          <form onSubmit={handleSaveMasterItem} style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
            {masterSubTab === 'kc' ? (
              <>
                <div>
                  <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                    Concept Keyword (key) *
                  </Label>
                  <Input
                    type="text"
                    required
                    placeholder="e.g. Form, Function, Causation..."
                    value={masterFormData.key}
                    onChange={e => setMasterFormData({ ...masterFormData, key: e.target.value })}
                    style={inputStyle}
                  />
                </div>

                <div>
                  <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                    Guiding Question (question)
                  </Label>
                  <Input
                    type="text"
                    placeholder="e.g. What is it like?"
                    value={masterFormData.question}
                    onChange={e => setMasterFormData({ ...masterFormData, question: e.target.value })}
                    style={inputStyle}
                  />
                </div>

                <div>
                  <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                    Definition / Explanation (definition)
                  </Label>
                  <textarea
                    rows={3}
                    placeholder="Provide concept definition..."
                    value={masterFormData.definition}
                    onChange={e => setMasterFormData({ ...masterFormData, definition: e.target.value })}
                    style={{ ...inputStyle, width: '100%', padding: '8px 12px', resize: 'vertical' }}
                  />
                </div>
              </>
            ) : (
              <div>
                <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                  Name / Statement *
                </Label>
                <textarea
                  required
                  rows={3}
                  placeholder={`Enter ${masterSubTab === 'ci' ? 'Central Idea' : masterSubTab === 'loi' ? 'Line of Inquiry' : 'ATL Skill'} description...`}
                  value={masterFormData.name}
                  onChange={e => setMasterFormData({ ...masterFormData, name: e.target.value })}
                  style={{ ...inputStyle, width: '100%', padding: '8px 12px', resize: 'vertical' }}
                />
              </div>
            )}

            <div style={{ display: 'flex', justifyContent: 'flex-end', gap: '8px', marginTop: '12px' }}>
              <Button
                type="button"
                onClick={() => setShowMasterModal(false)}
                style={{
                  background: 'none',
                  border: `1px solid ${borderColor}`,
                  color: textPrimary,
                  borderRadius: '6px',
                  fontSize: '13px'
                }}
              >
                Cancel
              </Button>

              <Button
                type="submit"
                style={{
                  background: textPrimary,
                  color: isDark ? '#09090B' : '#FFFFFF',
                  border: 'none',
                  borderRadius: '6px',
                  fontWeight: 600,
                  fontSize: '13px'
                }}
              >
                Save Item
              </Button>
            </div>
          </form>
        </Modal>
      )}

      {/* ------------------------------------------------------------- */}
      {/* COPY UNIT MODAL */}
      {/* ------------------------------------------------------------- */}
      {showCopyModal && (
        <Modal
          isOpen={showCopyModal}
          onClose={() => setShowCopyModal(false)}
          disableBackdropClose={true}
          title={`Copy Unit to ${currentSelectedClassObj?.kelas_nama || 'Class'} (${selectedYear || '2025/2026'})`}
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
            <p style={{ fontSize: '13px', color: textSecondary, margin: 0 }}>
              Select the source Academic Year, Class, and Unit you wish to duplicate. All attached LOIs, Key Concepts, and ATL Skills will be automatically replicated.
            </p>

            <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '12px' }}>
              <div>
                <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                  Source Academic Year
                </Label>
                <select
                  value={copySourceYear}
                  onChange={e => {
                    setCopySourceYear(e.target.value)
                    setCopySourceUnitId('')
                  }}
                  style={{ ...selectStyle, width: '100%' }}
                >
                  {years.map(yr => (
                    <option key={yr.year_name} value={yr.year_name}>{yr.year_name}</option>
                  ))}
                </select>
              </div>

              <div>
                <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                  Source Class
                </Label>
                <select
                  value={copySourceClassId}
                  onChange={e => {
                    setCopySourceClassId(e.target.value)
                    setCopySourceUnitId('')
                  }}
                  style={{ ...selectStyle, width: '100%' }}
                >
                  {pypClasses.map(cls => (
                    <option key={cls.kelas_id} value={cls.kelas_id.toString()}>{cls.kelas_nama}</option>
                  ))}
                </select>
              </div>
            </div>

            <div>
              <Label style={{ fontSize: '12px', fontWeight: 600, marginBottom: '6px', display: 'block', color: textPrimary }}>
                Select Source Unit to Copy *
              </Label>

              {sourceAvailableUnits.length === 0 ? (
                <div style={{ border: `1px dashed ${borderColor}`, borderRadius: '8px', padding: '20px', textAlign: 'center', color: textSecondary, fontSize: '13px' }}>
                  No units available in the selected Source Class & Academic Year.
                </div>
              ) : (
                <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', maxHeight: '240px', overflowY: 'auto', paddingRight: '4px' }}>
                  {sourceAvailableUnits.map(u => {
                    const isSelected = copySourceUnitId === u.id.toString()
                    const linkedLois = loiPivots.filter(p => p.unitId === u.id)
                    const linkedKcs = kcPivots.filter(p => p.unitId === u.id)
                    const linkedAtls = atlPivots.filter(p => p.unitId === u.id)

                    return (
                      <div
                        key={u.uniqueKey || `${u.isDedicated ? 'pyp' : 'topic'}-${u.id}`}
                        onClick={() => setCopySourceUnitId(u.id.toString())}
                        style={{
                          background: isSelected ? (isDark ? 'rgba(59, 130, 246, 0.15)' : '#E1F3FE') : cardBg,
                          border: `1px solid ${isSelected ? (isDark ? '#60A5FA' : '#1F6C9F') : borderColor}`,
                          borderRadius: '8px',
                          padding: '12px 14px',
                          cursor: 'pointer',
                          transition: 'all 0.1s ease',
                          display: 'flex',
                          justify: 'space-between',
                          alignItems: 'center',
                          gap: '12px'
                        }}
                      >
                        <div>
                          <div style={{ fontSize: '13px', fontWeight: 600, color: textPrimary, marginBottom: '2px' }}>
                            {u.title}
                          </div>
                          <div style={{ fontSize: '12px', color: textSecondary, lineHeight: 1.3 }}>
                            &ldquo;{u.centralIdea}&rdquo;
                          </div>
                          <div style={{ fontSize: '11px', color: textSecondary, marginTop: '4px', display: 'flex', gap: '12px' }}>
                            <span><strong>Theme:</strong> {u.theme}</span>
                            <span><strong>LOIs:</strong> {linkedLois.length}</span>
                            <span><strong>KCs:</strong> {linkedKcs.length}</span>
                            <span><strong>ATLs:</strong> {linkedAtls.length}</span>
                          </div>
                        </div>

                        {isSelected && (
                          <span style={{ fontSize: '12px', fontWeight: 700, color: isDark ? '#60A5FA' : '#1F6C9F', whiteSpace: 'nowrap' }}>
                            ✓ Selected
                          </span>
                        )}
                      </div>
                    )
                  })}
                </div>
              )}
            </div>

            {/* Target Destination Card */}
            <div style={{ background: isDark ? '#27272A' : '#F4F4F5', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '12px 14px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <div>
                <div style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.05em', marginBottom: '2px' }}>
                  Target Destination
                </div>
                <div style={{ fontSize: '13px', fontWeight: 600, color: textPrimary }}>
                  {currentSelectedClassObj?.kelas_nama || 'Class'} ({selectedYear || '2025/2026'})
                </div>
              </div>
              <span style={{ fontSize: '11px', padding: '4px 8px', borderRadius: '4px', background: isDark ? '#3F3F46' : '#E4E4E7', color: textPrimary, fontWeight: 500 }}>
                Copy Target
              </span>
            </div>

            {/* Controls Bar */}
            <div style={{ display: 'flex', justifyContent: 'flex-end', gap: '8px', marginTop: '12px', paddingTop: '12px', borderTop: `1px solid ${borderColor}` }}>
              <Button
                type="button"
                onClick={() => setShowCopyModal(false)}
                style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '13px' }}
              >
                Cancel
              </Button>

              <Button
                type="button"
                disabled={submittingCopy || !copySourceUnitId}
                onClick={handleExecuteCopyUnit}
                style={{ background: textPrimary, color: isDark ? '#09090B' : '#FFFFFF', fontSize: '13px', fontWeight: 600 }}
              >
                {submittingCopy ? 'Copying Unit...' : 'Execute Copy Unit'}
              </Button>
            </div>
          </div>
        </Modal>
      )}

      {/* ------------------------------------------------------------- */}
      {/* STRAND SEMESTER SCOPE CHANGE CONFIRMATION MODAL */}
      {/* ------------------------------------------------------------- */}
      {scopeConfirmModal.isOpen && (
        <Modal
          isOpen={scopeConfirmModal.isOpen}
          onClose={() => {
            if (!scopeConfirmModal.saving) {
              setScopeConfirmModal(prev => ({ ...prev, isOpen: false }))
            }
          }}
          disableBackdropClose={scopeConfirmModal.saving}
          title="Change Semester Scope"
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
            
            {/* Warning / Notification Banner */}
            <div 
              style={{ 
                display: 'flex', 
                alignItems: 'flex-start', 
                gap: '12px', 
                padding: '14px 16px', 
                borderRadius: '8px',
                background: isDark ? 'rgba(59, 130, 246, 0.12)' : '#EFF6FF',
                border: `1px solid ${isDark ? 'rgba(59, 130, 246, 0.3)' : '#BFDBFE'}`
              }}
            >
              <FontAwesomeIcon icon={faInfoCircle} style={{ color: isDark ? '#60A5FA' : '#2563EB', fontSize: '18px', marginTop: '2px', flexShrink: 0 }} />
              <div>
                <h4 style={{ fontSize: '13px', fontWeight: 700, margin: '0 0 4px 0', color: isDark ? '#93C5FD' : '#1E40AF' }}>
                  Confirm Curriculum Scope Adjustment
                </h4>
                <p style={{ fontSize: '12px', color: textSecondary, margin: 0, lineHeight: '1.45' }}>
                  Are you sure you want to change the teaching and assessment period for <strong>&ldquo;{scopeConfirmModal.strandName}&rdquo;</strong>?
                </p>
              </div>
            </div>

            {/* Scope Transition Flow Visual */}
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '14px', padding: '14px', background: isDark ? '#1F1F23' : '#F9F9F8', borderRadius: '8px', border: `1px solid ${borderColor}` }}>
              <div style={{ textAlign: 'center' }}>
                <span style={{ fontSize: '10px', color: textSecondary, textTransform: 'uppercase', fontWeight: 600, display: 'block', marginBottom: '3px' }}>
                  Current Scope
                </span>
                <span style={{ fontSize: '12px', fontWeight: 700, color: textPrimary, padding: '4px 10px', borderRadius: '4px', background: isDark ? '#27272A' : '#E4E4E7', display: 'inline-block' }}>
                  {getScopeLabel(scopeConfirmModal.currentScope)}
                </span>
              </div>

              <span style={{ fontSize: '18px', color: textSecondary, fontWeight: 700 }}>→</span>

              <div style={{ textAlign: 'center' }}>
                <span style={{ fontSize: '10px', color: isDark ? '#60A5FA' : '#2563EB', textTransform: 'uppercase', fontWeight: 600, display: 'block', marginBottom: '3px' }}>
                  New Scope
                </span>
                <span style={{ 
                  fontSize: '12px', 
                  fontWeight: 700, 
                  color: scopeConfirmModal.targetScope === '1' ? '#3B82F6' : scopeConfirmModal.targetScope === '2' ? '#A855F7' : '#10B981', 
                  padding: '4px 10px', 
                  borderRadius: '4px', 
                  display: 'inline-block',
                  background: scopeConfirmModal.targetScope === '1' ? (isDark ? 'rgba(59,130,246,0.2)' : '#EFF6FF') : scopeConfirmModal.targetScope === '2' ? (isDark ? 'rgba(168,85,247,0.2)' : '#FAF5FF') : (isDark ? 'rgba(16,185,129,0.2)' : '#ECFDF5'),
                  border: `1px solid ${scopeConfirmModal.targetScope === '1' ? '#93C5FD' : scopeConfirmModal.targetScope === '2' ? '#E9D5FF' : '#A7F3D0'}`
                }}>
                  {getScopeLabel(scopeConfirmModal.targetScope)}
                </span>
              </div>
            </div>

            {/* Safe Data Guarantee Reassurance */}
            <div style={{ fontSize: '12px', color: textSecondary, lineHeight: '1.45', background: isDark ? '#141416' : '#FAFAFA', padding: '10px 14px', borderRadius: '6px', border: `1px dashed ${borderColor}` }}>
              <strong style={{ color: textPrimary }}>🛡️ Your data is safe.</strong> Previously recorded student scores will not be lost.
            </div>

            {/* Modal Controls Bar */}
            <div style={{ display: 'flex', justifyContent: 'flex-end', gap: '10px', marginTop: '10px', paddingTop: '14px', borderTop: `1px solid ${borderColor}` }}>
              <Button
                type="button"
                onClick={() => setScopeConfirmModal(prev => ({ ...prev, isOpen: false }))}
                disabled={scopeConfirmModal.saving}
                style={{
                  background: 'none',
                  border: `1px solid ${borderColor}`,
                  color: textPrimary,
                  fontSize: '13px',
                  cursor: scopeConfirmModal.saving ? 'not-allowed' : 'pointer'
                }}
              >
                Cancel
              </Button>
              <Button
                type="button"
                onClick={handleConfirmStrandSemesterChange}
                disabled={scopeConfirmModal.saving}
                style={{
                  background: isDark ? '#3B82F6' : '#2563EB',
                  color: '#FFFFFF',
                  fontSize: '13px',
                  fontWeight: 600,
                  cursor: scopeConfirmModal.saving ? 'not-allowed' : 'pointer',
                  display: 'flex',
                  alignItems: 'center',
                  gap: '8px'
                }}
              >
                {scopeConfirmModal.saving && <FontAwesomeIcon icon={faSpinner} spin />}
                <span>{scopeConfirmModal.saving ? 'Saving Scope...' : 'Confirm Scope Change'}</span>
              </Button>
            </div>

          </div>
        </Modal>
      )}

      {/* ------------------------------------------------------------- */}
      {/* PYP REPORT CARD PRINT SELECTION MODAL */}
      {/* ------------------------------------------------------------- */}
      {showPrintModal && (
        <Modal
          isOpen={showPrintModal}
          onClose={() => {
            if (!isGeneratingPdf) setShowPrintModal(false)
          }}
          disableBackdropClose={isGeneratingPdf}
          title={`Print PYP Report Card — Semester ${printModalSemester}`}
        >
          <div style={{ display: 'flex', flexDirection: 'column', gap: '18px' }}>
            
            {/* Target Class Info Banner */}
            <div style={{ background: isDark ? '#27272A' : '#F4F4F5', border: `1px solid ${borderColor}`, borderRadius: '8px', padding: '14px 16px', display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '10px' }}>
              <div>
                <div style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '2px' }}>
                  Target Class &amp; Academic Year
                </div>
                <div style={{ fontSize: '14px', fontWeight: 700, color: textPrimary }}>
                  {currentSelectedClassObj?.kelas_nama || 'Class'} ({selectedYear || '2025/2026'})
                </div>
              </div>
              <span style={{
                fontSize: '11px',
                fontWeight: 700,
                padding: '4px 10px',
                borderRadius: '9999px',
                background: isDark ? 'rgba(59, 130, 246, 0.15)' : '#EFF6FF',
                color: isDark ? '#93C5FD' : '#2563EB',
                border: `1px solid ${isDark ? 'rgba(59, 130, 246, 0.3)' : '#BFDBFE'}`
              }}>
                Semester {printModalSemester}
              </span>
            </div>

            {/* Scope Selection: Class vs Single Student */}
            <div>
              <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '8px', display: 'block' }}>
                Print Scope / Target Report *
              </Label>
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '10px' }}>
                
                {/* Option 1: Entire Class */}
                <button
                  type="button"
                  onClick={() => setPrintScope('class')}
                  style={{
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'flex-start',
                    padding: '14px',
                    borderRadius: '8px',
                    border: `1.5px solid ${printScope === 'class' ? (isDark ? '#FFFFFF' : '#18181B') : borderColor}`,
                    background: printScope === 'class' ? (isDark ? 'rgba(255,255,255,0.06)' : '#F7F6F3') : 'transparent',
                    cursor: 'pointer',
                    textAlign: 'left',
                    transition: 'all 0.15s ease'
                  }}
                >
                  <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '4px' }}>
                    <FontAwesomeIcon icon={faLayerGroup} style={{ color: printScope === 'class' ? textPrimary : textSecondary, fontSize: '13px' }} />
                    <span style={{ fontSize: '13px', fontWeight: printScope === 'class' ? 700 : 600, color: textPrimary }}>
                      All Students in Class
                    </span>
                  </div>
                  <span style={{ fontSize: '11px', color: textSecondary, lineHeight: 1.4 }}>
                    {loadingPrintStudents ? 'Loading students...' : `Print all ${printStudentsList.length} students into one PDF file.`}
                  </span>
                </button>

                {/* Option 2: Single Student */}
                <button
                  type="button"
                  onClick={() => setPrintScope('single')}
                  style={{
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'flex-start',
                    padding: '14px',
                    borderRadius: '8px',
                    border: `1.5px solid ${printScope === 'single' ? (isDark ? '#FFFFFF' : '#18181B') : borderColor}`,
                    background: printScope === 'single' ? (isDark ? 'rgba(255,255,255,0.06)' : '#F7F6F3') : 'transparent',
                    cursor: 'pointer',
                    textAlign: 'left',
                    transition: 'all 0.15s ease'
                  }}
                >
                  <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '4px' }}>
                    <FontAwesomeIcon icon={faUserGraduate} style={{ color: printScope === 'single' ? textPrimary : textSecondary, fontSize: '13px' }} />
                    <span style={{ fontSize: '13px', fontWeight: printScope === 'single' ? 700 : 600, color: textPrimary }}>
                      Single Student
                    </span>
                  </div>
                  <span style={{ fontSize: '11px', color: textSecondary, lineHeight: 1.4 }}>
                    Choose 1 specific student to generate their report card.
                  </span>
                </button>

              </div>
            </div>

            {/* Single Student Dropdown Selector (if printScope === 'single') */}
            {printScope === 'single' && (
              <div style={{
                background: isDark ? '#1F1F23' : '#FAF9F5',
                border: `1px solid ${borderColor}`,
                borderRadius: '8px',
                padding: '14px 16px',
                display: 'flex',
                flexDirection: 'column',
                gap: '8px'
              }}>
                <Label style={{ fontSize: '11px', fontWeight: 600, color: isDark ? '#93C5FD' : '#1D4ED8', textTransform: 'uppercase', letterSpacing: '0.04em', display: 'block' }}>
                  Select Student to Print *
                </Label>
                {loadingPrintStudents ? (
                  <div style={{ padding: '8px 0', fontSize: '12px', color: textSecondary, display: 'flex', alignItems: 'center', gap: '8px' }}>
                    <FontAwesomeIcon icon={faSpinner} spin /> Loading class students...
                  </div>
                ) : printStudentsList.length === 0 ? (
                  <div style={{ padding: '8px 0', fontSize: '12px', color: textSecondary }}>
                    No students registered in this class. Sample preview student will be generated.
                  </div>
                ) : (
                  <select
                    value={printSingleStudentId || ''}
                    onChange={(e) => setPrintSingleStudentId(e.target.value)}
                    style={{
                      ...selectStyle,
                      width: '100%',
                      padding: '9px 12px',
                      fontSize: '13px',
                      fontWeight: 600,
                      cursor: 'pointer',
                      borderRadius: '6px'
                    }}
                  >
                    {printStudentsList.map((std, sIdx) => {
                      const fullName = `${std.user_nama_depan || ''} ${std.user_nama_belakang || ''}`.trim()
                      return (
                        <option key={std.user_id} value={std.user_id.toString()}>
                          #{sIdx + 1} {fullName}
                        </option>
                      )
                    })}
                  </select>
                )}
              </div>
            )}

            {/* Optional Custom Report Date Input */}
            <div>
              <Label style={{ fontSize: '11px', fontWeight: 600, color: textSecondary, textTransform: 'uppercase', letterSpacing: '0.04em', marginBottom: '6px', display: 'block' }}>
                Report Prepared Date (Optional)
              </Label>
              <input
                type="date"
                value={printReportDate}
                onChange={(e) => setPrintReportDate(e.target.value)}
                placeholder="Leave blank for default semester date"
                style={{ ...inputStyle, width: '100%', padding: '8px 12px', fontSize: '13px' }}
              />
              <span style={{ fontSize: '11px', color: textSecondary, marginTop: '4px', display: 'block' }}>
                Kosongkan jika ingin menggunakan tanggal default semester dari pengaturan sekolah.
              </span>
            </div>

            {/* Action Buttons */}
            <div style={{ display: 'flex', justifyContent: 'flex-end', gap: '8px', marginTop: '12px', paddingTop: '14px', borderTop: `1px solid ${borderColor}` }}>
              <Button
                type="button"
                onClick={() => setShowPrintModal(false)}
                disabled={isGeneratingPdf}
                style={{ background: 'none', border: `1px solid ${borderColor}`, color: textPrimary, fontSize: '13px' }}
              >
                Cancel
              </Button>

              <Button
                type="button"
                disabled={isGeneratingPdf || (printScope === 'single' && !printSingleStudentId && printStudentsList.length > 0)}
                onClick={handleExecutePrintPdf}
                style={{
                  background: isDark ? '#FFFFFF' : '#18181B',
                  color: isDark ? '#09090B' : '#FFFFFF',
                  fontSize: '13px',
                  fontWeight: 600,
                  display: 'flex',
                  alignItems: 'center',
                  gap: '8px',
                  padding: '9px 16px',
                  borderRadius: '6px'
                }}
              >
                {isGeneratingPdf ? (
                  <>
                    <FontAwesomeIcon icon={faSpinner} spin />
                    <span>Generating PDF...</span>
                  </>
                ) : (
                  <>
                    <FontAwesomeIcon icon={faPrint} />
                    <span>Generate &amp; Print PDF</span>
                  </>
                )}
              </Button>
            </div>

          </div>
        </Modal>
      )}

      {/* NOTIFICATION MODAL */}
      {notif.isOpen && (
        <NotificationModal
          isOpen={notif.isOpen}
          onClose={() => setNotif({ ...notif, isOpen: false })}
          title={notif.title}
          message={notif.message}
          type={notif.type}
        />
      )}

    </div>
  )
}
